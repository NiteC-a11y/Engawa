# secrets / local config（鍵を絶対にコミットしない）
.env
.env.local
.claude/settings.local.json

# python
__pycache__/
*.py[cod]

# sqlite（永続化導入時に必要なら個別に外す）
*.db

# editor / os
.DS_Store
Thumbs.db
.vscode/
.idea/

# 素材置き場（Gemini 生成の生画像。現行アプリは assets/chacha.png を使用）
/assets/raw/
