# Backlog — Engawa（縁側）

> 「これからやる動くタスク」だけを置く。確定事項は spec / adr / TECH_RULES にある。
> このファイルは未確定・未着手の在庫リスト。優先度なしのフラットなリスト。

---

## P3 — 双方向（実装済み・実機検証）
- [x] 話しかけて文脈が継続するか（「さっきの夕日」が通じるか＝長命セッション）（自動検証OK 6/27：前ターンの夕日を明示的に想起）
- [x] つぶやき中に割り込んだら止まってこっち向くか（session/cancel が実機で効くか）（自動検証OK 6/27：ambient が stopReason=cancelled で畳まれ、user ターンが主導権を取りハングなし）
- [x] 会話直後しばらく茶々が独り言で邪魔してこないか（QUIET_AFTER_USER）（自動検証OK 6/27：6s窓内 ambient 発火ゼロ→7.2sで再開）
- [x] 割り込み時の partial 出力（途中まで喋ったつぶやき）の見え方を確認（機構は確認。実機は初トークンまで ~1s 遅延あり、割り込みは「喋り出す前」に着弾しがち）
- [x] 〔見た目の小ネタ〕初トークン前に割り込むと空の「茶々 › 」行が残ってから turn-around マーカーが出る（6/27 修正）
  - 直し: `ConsoleView` のヘッダを遅延出力化＝`turn_start` では出さず、**最初の可視文字が来た時に初めて出す**。0チャンク（割り込み）や空白のみのターンはヘッダごと出ない（ANSI 消去に頼らずコンソール非依存）。改行畳み（6/27・persona「ひと続き」担保）と同じ状態機械に統合
  - 検証: 13件 PASS（割り込みシーケンスの出力 byte 一致＝marker→ユーザー返答のみ・空ヘッダ無し）
- [x] スラッシュ未実装コマンドの応答が自然か（/codex スタブ、/help、/quit）（自動検証OK 6/27）
- [x] **/arc 再生中に割り込みが効かない不具合（実装6/29・ユーザー報告）**＝`_play_arc_now` が `while True` でアーク完走までブロックしていて、その間 `on_user_input` が返らず入力ループが空かない＝再生中の話しかけが完走後まで処理されなかった（「[茶々がこちらを向いた]」が出ない）。**tick 駆動の `self.active` に載せ替えて即 return**（起→承→転→結は `_tick` が前進）に修正＝入力ループが空いて barge-in（cancel優先）が通る。副作用: /arc のビート間隔が 1s 固定→自然アーク同様 `ACTIVE_BEAT`(5〜12s) に。テスト `test_scheduler.TestArcInterruptible`(3件・載せ替え/再生中 barge-in/busy 拒否)。**自然な tick 駆動アーク／ambient の割り込みは元々動作（`test_cancel_priority_when_speaking`）＝今回は /arc 経路だけの修正**。
- [x] **「[茶々がこちらを向いた]」を本人が知らない不具合（実装7/13・ユーザー報告）**＝barge-in の演出が `view.system` だけ＝UI にしか出ず、茶々のセッションには何も流れないため、「こっち向いた？」と聞くと「知らん」と返る（演出層と文脈のズレ）。直し: `on_user_input` が `speaking` 中の割り込みで `user_narration(line, ctx, interrupted=True)` を注入＝ナレーション枠に「あなたは何か言いかけていたのを止めて、こちらを向いたところ。」の1行（`prompts._INTERRUPTED_FRAME`・barge-in は返答生成中もあり得るので「つぶやき」と断定しない）を「話しかけられた」枠の前に足す＝UI と世界が同じ事実を語る。復唱されても既存 echo-strip（injected marker）で消える。テスト4件（prompts 3・scheduler 1＋非割り込み時に語らない検証）＝全407 PASS
- [x] **first token 前の cancel → 次 prompt が内部エラーで茶々が無言（実装7/13・強制駆動ハーネスで発見）**＝barge-in が生成開始前（初トークン遅延 ~1s の窓＝実運用で当たりやすい方）に刺さると、履歴が user 止まりの宙ぶらりんになり、直後の prompt が claude-code-acp の内部エラー `-32603 [ede_diagnostic] result_type=user … stop_reason=null` で弾かれる。Scheduler はエラー応答を表示しない＝**「[茶々がこちらを向いた]」のまま返答が無言で消える**（従来から潜在・振り向き注入の E2E 検証中に踏んで発覚）。実機プローブで「1s 置いて同文再送で完全回復」を確認 → 直し: `AcpAgent.prompt` が内部エラー(-32603)を **1回だけ再送**して adapter 内で吸収（`_retryable_prompt_error`＋`PROMPT_RETRY_WAIT=1s`・部分発話後は再送しない＝二重発話防止・他エラーコードは素通し・ACP固有の癖なので Scheduler は触らず backend 中立維持）。テスト4件（回復/打ち止め/他コード素通し/部分発話後は再送せず）＝全411 PASS
- [ ] **（案・低優先）「打ち始め＝こっち向く」割り込みUI（ADR-0006 の体感改善）**＝現状の barge-in は *茶々が喋っている最中（speaking 中）* に被せた時だけ「[茶々がこちらを向いた]」が出る。ビート間隔が 5〜12s なので**人手でその窓を狙うのはほぼ無理**（＝割り込み「した感」が見えにくい。※打てない訳ではない・無言の間に打てば普通に返事）。案: **web で入力欄フォーカス／最初のキー入力の瞬間に `cancel` を送る**＝「キーボードに触れる＝茶々が手を止めてこっちを向く」。タイミング依存を解消し生き物らしさとも合う。難点: web 限定（console は Enter 前に検知できない）／キーに触れる度に止まる挙動になる点の調整。確認の現実解としては当面「自分で長い返事を誘発→喋ってる間にもう一行」で足りる。

## P4 — 客人来訪（v1 召喚来訪 済み 6/27）
- [x] codex-acp 接続（ChatGPT ログイン認証、OPENAI_API_KEY 除去で事故防止）
- [x] /codex <人格> で直接召喚（取り次ぎなし・即・確実）
- [x] 客人セッションは使い捨て（spawn → 数往復 → 破棄。辞去で内部 close）
- [x] 滞在を往復数で上限（GUEST_BEATS=3・無限ループ無し）※ADR-0015 Inc2 で**3人会話の部屋に置換**＝`GUEST_BEATS`/`next_phase` の旧一方向モデルは撤去（6/30）。有界は Room の `turn_cap`＋沈黙辞去が担保
- [x] 客人の人格を召喚時に動的注入（CLAUDE.md でなく prompt へ）
- [x] 客人出力をナレーション化して茶々に渡す（「塀の向こうの声」）
- [x] 客人の声をユーザーにも表示（6/27 修正）＝当初 codex のセリフは茶々への注入 text に埋まるだけで画面に出ず「codex が喋らない」状態だった。`Narration.voice` に生セリフを載せ、`ConsoleView` が `客人 ›` 行で即表示（茶々が黙っても客人は見せる・声は1行に畳む）。召喚 spawn 失敗も可視化（「客人は来られなんだ…」）。voice 14件＋全5スイート PASS、実 codex 召喚 E2E で `客人 ›`→`茶々 ›` 往復を確認
- [x] アーク中の召喚が弾かれる問題（6/27 修正）＝箱庭アーク進行中は `active` 占有で `/codex` が「今は手が離せん」で拒否されていた。方針確認の上「アークを畳んで即通す」に変更：箱庭アークなら `_conclude`（reset＋cooldown）で畳んで客人を通す／客人来訪中は重ねない（断る）／喋り中は cancel 優先で止める。併せて tick ループと召喚が `self.active` を同時駆動する競合を `drive_lock` で直列化（cancel はロック前に出して barge-in 維持）。preempt 14件＋全6スイート PASS
- [x] 自発来訪トリガー（夕方以降 × 低確率 × クールダウン、来ない日がある）＝`GuestSource.eligible` 実装＋registry 登録（6/27：`persona=None`＝自発インスタンス、`eligible`＝`hour>=15`×`random<GUEST_VISIT_PROB`、`reset` で役を抽選、`default_sources(spawn_codex=…)` で registry へ。fake で単体18/統合8件 PASS。**実 codex 自発 E2E も成功（6/27）**＝強制駆動ハーネスで実 resident＋実 codex を spawn、役「野良の絵描き」を自動抽選→到着/世間/辞去を codex が実生成→茶々が関西弁で実反応→3/3ビート・cooldown=20・teardown 正常）
  - つまみ: `ENGAWA_GUEST_FROM_HOUR`(既定15) / `ENGAWA_GUEST_PROB`(既定0.05・低め)。cooldown は `GuestSource.cooldown_ticks=20`
  - [x] **自発来訪が殆ど来ない不具合（実装6/29・ユーザー報告）**＝自発来訪を箱庭アークと同じ抽選経路に乗せていたため `arc_prob(0.30) × prob(0.05) × random.choice 競合` の**三重ゲート**で実効レートが ~0.005/tick まで間引かれ、`prob` が「tickあたりの素確率」という説明と乖離（≒数時間に1回）。`_tick` で**自発来訪を arc 抽選から独立に判定**（`prob` が実効 per-tick 率・from_hour と cooldown だけが条件・箱庭アーク抽選は guest を除外）に変更。これで `prob=0.05` でもざっくり数十分間隔（+cooldown 20tick）になり、`prob` を上げれば素直に頻度が上がる。テスト `test_scheduler.TestAutonomousGuestVisit`(3件・arc抽選非依存/from_hour/cooldown)。`engawa.json.sample` の prob コメントも実態に整合。
- [x] 客人の世間話に外部トピックを注入（ADR-0014・6/27）＝取得先ホワイトリストは **`topic_sources.json`（リポジトリ直下・config 主導／env `ENGAWA_TOPIC_CONFIG` で差し替え・コードに URL を埋めない）**。rss は `url`＋自己申告 `domain` 一致＋https＋見出し限定＋長さ/本数/サイズ上限、設定に載った源しか fetch しない＝設定が whitelist（欠損は時節 local へフォールバック）。既定は**時節(local)稼働**（二十四節気＋旬・無取得）、世間ビートに確率（`ENGAWA_TOPIC_PROB`既定0.7）で『種』として注入＝新聞調禁止・データ枠付け。30分更新（`ENGAWA_TOPIC_REFRESH_MIN`）。topics 26件＋全8スイート PASS。**実 codex E2E でトーン確認**（ご隠居が旬を自然なうわさ話に・新聞調ゼロ・茶々も具体反応）
  - [x] **やわらかRSS の実 URL 精査＝現行 feed 無しと確定（7/9）**＝縁側トーンに合う生きた RSS は見つからず＝**当面 時節(local) で確定**（rss 取得機構＝`_fetch_rss`/`_parse_rss_titles`/whitelist は健在＝将来 feed が見つかれば config に1行足すだけで有効化できる）。将来の再挑戦時は「まだ生きてる柔らかい和文 feed があるか」から。`topic_sources.json` の `暮らしコラム例`(enabled:false) は機構のサンプルとして温存。
  - [x] **七十二候で local を厚く（7/9）**＝RSS の代わりに「季節の鮮度」を**ネット非依存**で足す。`sources.KOU`（72候・略本暦・`(月,日,名,読み,一言)`・暦年昇順 1/1冬至末候〜12/27冬至次候）＋`_current_kou`（SEKKI と同じ「直近手前」選び）を追加し、`_seasonal_topics` が**節気・七十二候・旬の3種**を返すように。二十四節気(約15日)より細かい**約5日刻み**で種が rotate＝季節の手触りが増え、pick の候補も増えて粘着も緩む。種の見え方例「七十二候は今「温風至（あつかぜいたる）」—夏の熱い風が吹き始める頃」（読みも載る＝縁側の柔らかさ）。テスト `TestSeventyTwoKou`(7件・72件/昇順/重複無/境界日/年跨ぎ wrap/種の名＋読み)＋既存の季節shape 2件を 2→3 に更新＝**全367 PASS・ruff clean**。トーンは要 実 codex 目視（季節ネタが自然にうわさ話になるか）。
    - 精査メモ(6/27): NHK 全カテゴリ実取得＝硬い時事/災害でトーン不一致＝不採用寄り（cat3 文化が一番マシだが混在）。本命候補 **tenki.jp サプリ（季節・暮らし/七十二候コラム）= 縁側のトーンに合致**だが、2010年告知の RSS サービスページ(`tenki.jp/webservice/rss/`)は **404**＝現行フィード URL が要特定（`tenki.jp/suppl/` の feed リンク or 別の柔らかい源を当たる）。当面 時節(local) だけで十分弾むので急がない。自作 `_parse_rss_titles` は RSS/Atom 実フィードで動作確認済み
    - 精査メモ(7/9): 実フェッチで確定＝**tenki.jp の RSS サービスは 2014-05-13 に終了済み**（公式アナウンス外の第三者記録で確認）。`tenki.jp/suppl/` は 200 で本文は取れるが **feed autodiscovery も rss/feed/atom 参照もゼロ**、候補 URL(`/suppl/rss` `/suppl/feed` `/rss` `/feed` `/suppl/rss.xml`)は全て **404**。代替の柔らか季節系も空振り＝**暦生活(543life.com)**＝feed autodiscovery 無し・`/feed` 404・`?feed=rss2` は HTML(titles=0)／**All About(allabout.co.jp)**＝`/rss/` は HTML(titles=0)・他 404。RSS 自体の衰退で柔らか和文 feed は実質絶滅。→ local時節で十分（Backlog の当初判断どおり）。
  - [x] **人格マッチ topic の源拡充（7/1）**＝`topic_sources.json` の `kind:"local"` に inline `topics`＋`persona` を持たせる仕組み（`_local_topics`）を追加し、行商人→相場/値・絵描き→色/光・ご隠居→昔話/近所・風流人→句/季語・旅人→道中 の5役ぶんを既定投入。**併せて人格マッチの方向を修正**（`persona in t["persona"]` は実 persona＝長い句で不発だった→`_persona_matches`＝タグが役名の一部に含まれれば一致・「行商」⊂「…行商人」）。候補が2→7に増え「ネタ2個で直近回避クランプ」も自然緩和。**実 codex で役ごとに色が変わるのを確認（7/1）**。
  - [x] **同じ話題への粘着対策（7/1・実機報告）**＝色分けは効いたが客人が同じ話題を粘る。(1) 種の**クールダウン** `TOPIC_COOLDOWN`（`topic.cooldown`・既定2＝種を置いたら次まで客人ターンを空ける・毎ターン振らない）(2) `guest_air` に「深追いせず話は移してええ・前に出た話は繰り返さん」を追記。`_topic_recent`（重複回避）と合わせ三重。テスト `test_scheduler`（cooldown で3ターン中1回だけ種）＋`test_sources`（anti-dwell 文言）＝全214 PASS。**残: 実 codex で粘着が減ったかの目視。**
  - [x] **客人トピック注入を Room 経路へ復活（regression・6/30 → 7/1 実装）**＝ADR-0014 のトピック注入は旧一方向来訪モデル（`GuestSource.next_phase`→`_pick_topic`/`_topic_instr`）に実装されていたが、Inc2 で客人が 3人会話の部屋（`conversation.Room`）に移った際、`room_guest_prompt(persona, window, kind)` が ctx/topics を受け取らないため**休眠＝実質ロスト**していた（A2 6/30 で旧モデルごと削除）。**ambient「縁側の空気」方式で復活**（ユーザー方針＝機械的注入でなく、天気と同型に種を頭の隅へ置き、口に出すかは常に codex 判断＝会話が自然に流れた時だけ発火）。実装: 選定＝`sources.pick_topic_text`(純関数・確率/履歴なし)／文言＝`prompts.guest_air`＋`room_guest_prompt(air=None)`(後方互換)／確率(`TOPIC_PROB` を「種が空気に混じる確率」へ転用)・直近回避(`Scheduler._topic_recent`)・場面判定(**CHIME/REPLY のみ**・到着/辞去は入れない)＝`scheduler.guest_say`。`conversation.Room` は不変。取得基盤は健在＝再利用。テスト13件追加(sources 10・scheduler 3)＝全196 PASS。ADR-0014 に「更新（ambient 再設計）」節・engawa.json.sample の prob コメント整合。**残: 実 codex E2E で“たまに自然に季節ネタ”のトーン確認（ユーザー）**。
- [x] **3人会話の時間感覚ズレを修正（7/1・実機報告）**＝夜22時なのに「夕暮れ」「日が落ちる前に」等。原因＝room プロンプトに現在時刻が無く persona 名の時間帯（例「夕暮れに道を訪ねてきた旅人」）に引っ張られる。`prompts.ambient_line(ctx)`（時刻＋天気＋「設定より今を優先」）を `room_guest_prompt`/`room_resident_prompt` 冒頭へ必ず注入（scheduler が build_context で ctx を渡す）。天気も ambient_line に集約し guest_air は種のみに簡素化。テスト（ambient_line・room prompt に時刻・scheduler で両者に注入）＝全218 PASS。
- [x] **客人が API エラーの生 JSON を縁側に喋る不具合を修正（7/9・実機報告・スクショ）**＝codex が 400（`unsupported_value`・"This model is not supported when using X-OpenAI-Internal-Codex-Responses-Lite."）を **agent_message_chunk として本文で流し**、`AcpAgent.prompt` がそれを本文として返す→`_guest_say` が `客人 › { "type":"error", … }` とそのまま表示＋transcript も汚染していた。純関数 `prompts.is_error_payload`（先頭`{`＋error/type シグネチャ・途中切れも検出）を追加し、`room_speakers._guest_say` がエラー応答を検出したら**生 JSON を出さず `guest_timed_out=True`＝既存の急用退場（`guest_timeout_leave`）で世界観を保って畳む**（持続エラー＝モデル非対応でも即クリーンに退場・codex kill）。テスト `TestIsErrorPayload`(6件・実ペイロード/error形/途中切れ/正常セリフ非検出)＋`test_room_speakers`(客人エラー→退場フラグ)＝全374 PASS・ruff clean。**素因（別口）＝codex のモデル指定が非対応**（`ENGAWA_CODEX_MODEL`／codex-acp のルーティング）＝`/model` と env を確認。残: 実 codex で退場演出の目視。
- [ ] 時節の挨拶差し替え（月初・季節の変わり目・祝日）※茶々の自発挨拶側（客人ネタとは別口）
- [ ] **（構想）箱庭アーク「新聞」＝茶々が新聞をのぞき込む（7/2 ユーザー着想「茶々が新聞読むシーンもあってええね ｗ」）**
  - **実装**: `sources.default_sources` に `BoxGardenArc("新聞", gate=…, phases=[…])` を1個足すだけ（雀/猫/風と同型・Open-Closed）。gate 案＝朝っぽい時間＆雨でない（新聞＝朝・紙が舞うのは晴れ）。下書き: 起「一枚の新聞紙が風に乗って縁側にふわりと舞い込んできた」／承「茶々はその上にのそりと乗っかって見出しをじっと見下ろしている」／転〔下記〕／結「読むのに飽きたか、新聞紙の上でくるりと丸くなった」。
  - **おもしろい接続＝topics 再利用**: いま季節ネタ(`fetch_topics`)は**客人しか使っておらず、客人には"新聞調禁止"**（うわさ話で振れ）。だが**茶々が見出しを猫なりに半分わかったフリで呟く**のは、その禁じた新聞調の"正しい/可愛い版"＝客人経由とは別ルートで季節ネタが世界に出る（2系統が繋がる）。転ビートの案: **(A) 実 topic を差す**（見出し＝fetch した季節ネタ・`_neko_ten` 同様の callable で ctx["topics"] から拾う）／**(B) 純ネタ**（「風がめくった三面記事に前足をちょん」だけ・topics 非依存）。まず B で入れて後で A を足すのが安全。
  - **注意**: アークを増やすとテンポ観測の箱庭構成が変わる＝**代打/テンポの半日観測が済んでから入れる**。テストは既存 `BoxGardenArc` 同様（phases 進行・gate・結了）。
- [ ] **（構想）客人のバックエンドを選べるようにする＝Claude 客人（`/claude <人格>`）**（7/2 ユーザー着想）
  - **動機**: (1) codex(ChatGPT) 認証が面倒/不安定な時に **Claude だけで3人会話・代打をテスト**できる (2) 「Claude 同士の掛け合い」も試したい。
  - **調査結果（現状）**: 客人は `/codex` 専用＝`acp.AcpAgent.spawn_guest` が **codex-acp**（`ENGAWA_CODEX_CMD`=`npx -y @agentclientprotocol/codex-acp`）を起動。住人 茶々は `spawn_resident`＝**claude-agent-acp**（`ENGAWA_ACP_CMD`）で cwd の CLAUDE.md により人格注入。**両方とも ACP エージェント**なので、住人/客人の違いは「どの adapter を起動するか＋人格の入れ方」だけ。認証はどちらもサブスク＝**追加課金なし**。
  - **技術的な要点/注意**: Claude 客人を足すなら claude-agent-acp を起動しつつ **茶々の CLAUDE.md を読ませない**（cwd を分ける等。読むとその子も“茶々”になる）で、codex と同じく **人格を prompt で動的注入**（`spawn_guest` の人格注入経路を claude 版にも用意）。モデルは住人=`ANTHROPIC_MODEL` / codex=`CODEX_CONFIG` の既存 `_model_env` 系に倣う。関連負債 [茶々用 CLAUDE.md の別ディレクトリ運用（L139）] と地続き。
  - **設計判断**: ADR-0008 は **客人＝「別世界から来た違うAI」= Codex**（異質さが味付け）が前提。Claude 客人はこの前提を広げる＝**新 ADR で「客人のバックエンドは選べる（既定は codex＝異質さ重視）」と残す**のが筋。実装候補: ①`/claude <人格>` を `/codex` と並べて追加 ②`guest.backend`(codex|claude) を config 化＋両コマンド対応。
  - **優先度**: 低（今は代打の目視テスト優先）。codex 抜きでテストしたい実需が続くなら軽い ① から。
- [~] 〔大物・ADR-0015〕客人(visitor)に **人間アンカーで有界な3人会話** を解禁（私↔茶々／私↔客人／茶々↔客人／3人 の全組合せ）。「部屋」方式＝全員が全員の発言を聞く・宛先で応答者が決まる（取り次ぎでない）。歯止め: 人間アンカー・連続AIターン上限・来訪は有界のまま。最難関は**ターン管理**。
  - 決定（6/28）: 宛先=名前メンション＋既定茶々／連続AIターン上限=2手（ADR-0015 実装メモ）
  - [x] **Inc1**: 会話エンジン `conversation.py`（State: Greeting→AwaitingHuman⇄Responding→Leaving→Closed。AwaitingHuman は tick で AI を動かさない＝自律往復が起き得ない／Responding は cap 手で必ず人間待ちへ）。Speaker(Strategy/DI)・resolve_addressee(純関数)・Transcript(value)。ユニット17件
  - [x] **Inc2**: Scheduler/GuestSource 結線（`/codex`・自発来訪が部屋を開く・codex/茶々の双方に transcript window を渡し双方向化・`view.say` で確定発話を一様表示・沈黙で辞去・codex 使い捨て維持）。統合テスト5件。全84 PASS・web JS node --check OK
  - [~] **Inc3**: ~~cancel優先(ADR-0006)の部屋内統合~~＝**スコープM 実装済み（7/18・ADR-0031）**＝tick 駆動チェーン（挨拶/代打/辞去）中の被せが即効く（generation 無効化＝`_room_rev`＋`Room._utter` の commit gate＋in-flight cancel。言いかけは表示/transcript に積まない・演出「[話の途中でこちらを向いた]」・timeout 誤発火抑制・fill 予算は不発なら返す）。設計→codex 二段レビュー（`codex/review-2026-07-18-room-cancel.md`）で bool フラグの競合と「run() の入力は逐次＝入力起点チェーン中は連打が読まれない」を補正／茶々の room ストリーミング（今は `_room_resident_speak` が全文返却→`view.say` 確定行表示。RESIDENT_GUARD 既定でソロも非ストリームの間は見送り）／~~実 codex の3人会話 E2E（実機・ユーザー）~~＝**済（7/18 ユーザー確認。room 実運用由来の fix が裏付け＝2fae124 時間感覚ズレ・642255a APIエラー急用退場・ADR-0025 代打実装）**／表示・スプライト演出の磨き
    - [ ] **スコープL（保留・ADR-0031 検討済み）**: room 入力だけ background worker 化＋per-prompt turn token＋forced-cancel 後の客人 respawn ＝自分の発話への応答チェーン中の**連打**にも barge-in を効かせる完全版。M で実機に暮らし「応答中の連打の待ち」がまだ痛ければ着手（設計は codex レビュー済み・ADR-0031 備考）
    - [x] **barge-in 実機E2E（ユーザー）**＝**済（7/18・代打中に被せて即畳めるのを実機確認）**。codex-acp の session/cancel は初回観測では問題なし（挙動の癖が出たら ADR-0031 備考の respawn 対策へ）
    - [x] **codex diff レビューの指摘【高】fix（7/18）**: 中断不可の手（ARRIVE/辞去）にも cancel が飛び、部分文が preemptible=False の gate を素通りして commit され得た → `Room.utter_preemptible`（生成中の手の中断可否を公開）＋ `_room_barge_in` は rev+1 のみ常時・cancel/演出は中断可の手だけ。統合テスト3本＋単体1本＝451 green。**テスト脱漏の原因分析は ADR-0031 追記**（層またぎ不変条件が層別テストの継ぎ目に落ちた・fake のブロック位置が想定をなぞった・窓の列挙不足）
    - [x] **客人がせわしない（沈黙ですぐ辞去）を緩和（6/29・ユーザー報告）**＝`idle_leave_ticks` を 4→**8**（来訪中tick 5〜12s なので ~20-48s→~40-96s）にし、**config つまみ化**（`guest.idle_leave_ticks`/`ENGAWA_GUEST_IDLE_LEAVE`・scheduler から Room へ注入）。話しかければリセット・有界は維持。テスト `test_scheduler`（しきい値前は居座る／沈黙継続で辞去・値非依存化）。
    - [x] **人間が入らないと無音で待たれて寂しい → 茶々が“人間役の代打”で場をつなぐ（7/2・ユーザー報告・ADR-0025）**＝`AwaitingHuman` で沈黙が `fill_after`（既定2tick≒15s）続くと `ResidentFilling`（茶々 MUSE→客人 REPLY の1往復）で場を回し、必ず人間待ちへ戻る。**予算 `fill_cap`（既定3）で人間不在の連続AIターンを厳密に上限**＝使い切れば `idle_leave_ticks` で辞去（終端保証）。人間が関与すると予算リセット。`fill_cap=0` で従来の純待ちへ退避可。config: `guest.fill_cap`/`fill_after_ticks`・`ENGAWA_GUEST_FILL_CAP`/`_FILL_AFTER`。テスト: conversation 5件（発火/枯渇→辞去/人間リセット/純待ち/無言スキップ）・scheduler 2件（代打が実プロンプトを流す/長引いても必ず辞去）。全224 PASS。**残: 実 codex/resident で代打のトーン目視（もてなし過ぎない自然な間か）**。
      - [x] **テンポを一定でなく減衰させる（7/2・ユーザー着想「人間は来た直後が一番賑やか→ネタ切れでゆっくり→帰る」）**＝代打の間隔を回ごとに延ばす（n回目しきい値=`fill_after + n×fill_slowdown`・既定 slowdown=1）。静的に1つの間隔値を選ぶ悩み（速すぎ/遅すぎ）を時間変化で解消。人間が関与すると予算リセット＝間隔も先頭へ（賑わい復活）。`fill_slowdown=0` で一定。config `guest.fill_slowdown`/`ENGAWA_GUEST_FILL_SLOWDOWN`・ADR-0025 に追記。テスト conversation 3件（減速/一定/人間リセット）＝全227 PASS。ビート秒は `timing.active_beat_min/max` に掛かる。**残: 実機で減衰カーブの体感目視。**
      - [ ] **（負債・様子見中 7/2）会話テンポつまみが増え過ぎ＝手調整が難しい**（ユーザー指摘「パラメーター調整が難しいアプリになってきた ｗ」）。今 `active_beat_min/max`・`fill_cap`・`fill_after`・`fill_slowdown`・`idle_leave_ticks` が **掛け算で絡む**＋単位が「tick×秒」＋**再起動必須**で試行ループが遅い。**方針（合意）: つまみを増やす/賢くするのでなく畳む**。①まず実機で"当たり"の1組を見つける（**今ここ・様子見**）→ ②いい値が出たら**既定に焼き込み**、個別つまみは engawa.json.sample のコメントへ退避＝本体から意識を消す → ③将来 **プリセット化**（`のんびり/ふつう/賑やか` 等）＋できれば **`/tempo` ライブ切替**（`/font` と同じ poll 方式・再起動不要）で **P5 の歯車⚙設定パネル(B)** に束ねる。**急いで作らない＝当たりを掴むのが先**。関連: [UI 設定をアプリ内で（L68〜74）]。

## P5 — ドット絵UI（Increment 1 済み 6/27）
- [x] **Increment 1: 最小の実UI**（6/27）＝`views.WebView`（poll 方式・ADR-0013 の View 差し替え）＋`engawa_main` web モード（`ENGAWA_UI=web`。webview メインスレッド／Scheduler 別スレッド+loop／窓閉じで signal_close→teardown）。出力は `_log`＋rev 差分を JS が poll、入力は `queue.Queue`。茶々の lazy 表示・改行畳み・客人 voice 表示も ConsoleView と揃え。WebView 12件＋全7スイート PASS。**実窓 GUI 起動は未（GUI ブロックのため手元検証はユーザー）**
  - [x] つぶやき欄のストリーミング表示（agent_message_chunk → poll で逐次）
  - [x] 入力欄（通常テキスト＋スラッシュ）を UI に（6/27 修正: 自分の発言を `あなた ›` で表示エコー＝web は端末と違い入力が自動表示されず「コメントが消える」バグだった。`send()` でログに積む。往復テスト#9 で再発防止）
  - [x] 段階導入の第一段＝枠ありウィンドウ
  - [x] 茶々 procedural アニメ（6/27・ADR-0010 の「骨」）＝まばたき/耳ピク/尻尾/胴体ゆらり（idle）＋話す＝ぴょこぴょこ/前のめり、客人来訪＝耳ピン、話しかけ＝こっち見る。state は poll（kind/voice/done/live）から JS が推定（Python は `kind` を1個足すだけ）。この state→動きの配線は Increment 3 の Aseprite 差し替え後も流用。JS は node --check 済み
  - 起動メモ（cmd）: `set "ENGAWA_UI=web" && python src/engawa_main.py`（`$env:` は PowerShell 専用・cmd は `set`。空白混入回避でクォート）
- [x] Increment 2: frameless + on_top + 画面隅へ配置固定（6/27）＝`create_window(frameless=True, on_top=True, resizable=False)`、起動後 `_screen_size()`(webview.screens→ctypes)＋`corner_xy()` で隅へ `move`（`ENGAWA_UI_CORNER` br/bl/tr/tl・既定 br）。透過なし（ADR-0009）。タイトルバー喪失の補い＝scene に `pywebview-drag-region`（掴んで移動・効かない時 `ENGAWA_UI_EASYDRAG=1` で全面ドラッグ）＋右上×ボタン（`api.close`→`window.destroy`）、`/quit` でも窓を閉じる配線。corner_xy/close は unit、新パラメータ＋隅配置は実機スモークで例外なし（3072x1728 で右下算出確認）
  - [x] 実機の見た目/操作 確認（6/27・ユーザー）: 高DPI(3072x1728)でも右下に正しく収まり、scene ドラッグ移動・on_top 前面維持・×/`/quit` 閉じ すべて OK
  - [x] **起動時の窓ちらつき解消（7/3・ユーザー報告→確認）**＝当初は既定位置(中央)で `create_window` → 起動後 `move` で隅へ寄せていたため、中央で一瞬出て隅へ飛ぶ「フラッシュ」が見えた。`_screen_size()`＋`corner_xy()` を **`create_window(x,y)` に前渡し**して最初から隅に生成（`engawa_main.run_web`）。起動後の `move` は多モニタ用の精密補正として残置（隅→隅で不可視・失敗しても位置ズレ止まりで不可視化しない）。※「一瞬窓」を最初 subprocess コンソールと誤診→遠回り（教訓メモ）。
  - [x] **文字が小さい/窓が狭い対策（6/30・ユーザー報告）**＝隅窓の幅/高を **config つまみ化**（`ENGAWA_UI_W`/`H`(既定400×520・以前360×480)・`engawa.json[ui]`・env>json>既定）＋**`resizable=True`**（Inc2 の `resizable=False` を更新・`min_size`(240,240)・frameless ゆえ実挙動は目視）。既存 `corner`/`easydrag` も env 直読み→`config` 経由に寄せ engawa.json[ui] 対応。`run_web` から `_ui_config`/`_web_window_kwargs` を純関数抽出。文字は本文 `#log` を 13→15px に。テスト `test_views.TestUiWindowWiring`/`TestBuildWebHtml`。
    - ⚠ **当初入れた `ENGAWA_UI_ZOOM`(html{zoom}) を即時撤回（6/30・ユーザー報告）**＝frameless＋`#app{height:100vh}`＋`overflow:hidden` で UI 全体を拡大すると**下の入力欄が窓外へはみ出て消える＝操作不能**になった（GUI 未検証のまま入れた事故）。zoom 機構を撤去し、レイアウトを壊さない**本文フォント拡大(`#log`15px)**へ置換（`#log` は overflow-y:auto＝スクロールするので入力欄は切れない）。教訓＝GUI 挙動はユニットで担保不可・目視必須（原則7/ADR-0019）。
    - [x] **リサイズ手段を実装（6/30・ユーザー報告「リサイズできない」→実機OK）**＝`resizable=True` だけでは frameless 窓に掴む縁が無くドラッグ不可だった。**右下に `#grip`**（nwse-resize）を置き、JS の pointer ドラッグ→`pywebview.api.resize`→`WebView.resize_window`→`window.resize`（min 240 クランプ・画面座標で差分）で実装。テスト `test_views.TestWebViewResize`(3件)＋`TestBuildWebHtml.test_has_resize_grip`、JS は node --check OK。**実機でドラッグ・リサイズ動作をユーザー確認（6/30）**。
    - [x] **文字サイズを config つまみ化（6/30・ユーザー要望「目が悪い人向け」）**＝`ENGAWA_UI_FONT`(既定1.0・0.8〜2.2)。本文/入力(`#log`/`.sys`/`.who`/`#in`)のフォントだけを `calc(BASE * var(--fz))` で拡大（`build_web_html(font)` が `:root{--fz:N}` を注入）。`#log` は overflow:auto＝スクロール、`#bar` は flex＝**入力欄を押し出さない**（窓全体 zoom の事故を踏まえた安全策）。テスト `test_views.TestBuildWebHtml`(font注入/既定/入力欄存続)＋`TestUiWindowWiring`(env)、JS node --check OK（全163 PASS）。**見た目はユーザー目視**。
    - [~] **UI 設定をアプリ内で変更可能に（6/30・ユーザー要望「env 変更は不便」）**＝今は `ENGAWA_UI_FONT`/`W`/`H` を env か `engawa.json` 編集＋再起動でしか変えられず不便。アプリ内から**ライブ**で調整したい。**2系統（補完）**:
      - [x] **(A) `/font <倍率>` スラッシュ（7/1 実装）**（例 `/font 1.4`・`/font` で現在値・`/font save` で永続化）＝ADR-0007「スラッシュ＝縁側操作（茶々に流さない）」に乗る・**本窓をごちゃつかせない**・既存入力欄で打つので **IME 切替不要**。**明示保存方式**（ユーザー選択）＝数字はそのセッションだけライブ適用（`--fz` 差し替え・再起動不要）、`/font save` で `config.set_value` が `engawa.json[ui].font` へ書き戻す（localStorage 禁止と両立・config 主導）。ライブ適用は cross-thread `evaluate_js` を避け **poll に `font` を載せる方式**（本窓＋観戦窓）。console は端末フォント依存で no-op（注記）。`ENGAWA_UI_FONT`(env) が立つと save 後も env 優先＝保存時に告知。クランプ 0.8〜2.2（`_ui_config`/`scheduler.UI_FONT_MIN/MAX`）。テスト: `test_config.TestConfigSetValue`(4)・`test_views.TestFontLiveApply`(6)・`test_scheduler.TestFontCommand`(6)＝全183 PASS・JS node --check OK。**見た目はユーザー目視**。**残: `/win <w> <h>` 相当の窓サイズのアプリ内調整（同じ `api.resize` を叩く）**。
      - **(B) 歯車アイコン⚙→設定パネル**（既存の×ボタンの隣に小さく置く）＝クリックで小オーバーレイを出し、文字サイズ(スライダ/A−A＋)・窓サイズ等を**スライダ/ボタン**で調整。**発見しやすい＝アクセシビリティ向き**（"目が悪い人" はスラッシュを知らない）。パネルは一時表示で常時クラッタにはならない（アイコン1個の追加は許容・memory「本窓をごちゃつかせない」とのトレードオフは小）。手動操作・IME 不要で嗜好に合致。
      - A/B は**同じ適用関数を叩く**形に。ライブ適用は容易：font は JS で `document.documentElement.style.setProperty('--fz', n)`（**再起動不要**）、窓は既存 `api.resize`。
      - 代替: キーボード（Ctrl +/− で文字・Ctrl 0 で戻す）。
      - 永続化は **localStorage 不可（TECH_RULES §7）**＝Python 側で `engawa.json[ui]` に書き戻す（config 主導と整合）か、当面セッション内のみ。
      - console 版にも `/font` を効かせるか（console は端末フォント依存＝No-op か注記）は要検討。
    - [x] **観戦窓(GAME_HTML)も同倍率で拡大（6/30・ユーザー報告「ゲーム版が拡大されず不自然」）**＝当初 font は本窓(WEB_HTML)だけで、観戦窓は生 GAME_HTML＝拡大されず不整合だった。GAME_HTML に `--fz` を注入（文字＋カード箱 `.card`＋行を `calc(BASE * var(--fz))` で揃えて拡大）し、`build_game_html(font)`＋`set_layout(...,font)`＋`game_open` で**窓サイズも font 倍**に（はみ出し防止・`#app{overflow-y:auto}` も安全網）。テスト `test_views.TestGameWindowFont`(4件)＋GAME_HTML JS node --check OK（全167 PASS）。**見た目はユーザー目視**。
- [x] Increment 3: スプライト差し替え機構＋仮の皮（6/27）
  - ★**現行（2026-07-04 刷新）**: 以下は旧履歴。今は Gemini 三毛猫の**4表情正規化シート**(`chacha.png` 704×176・0平常/1口開け/2目つむり/3反応)＋**縁側背景 `scene.png`**(障子＋板の間)。サイズは `sprite.json[display_px]` で縮尺（絵は固定・CSSスムーズ縮小）、接地影は `shadow_w/dy/h`、背景/茶々とも `ENGAWA_SCENE_BG`/`ENGAWA_SPRITE_CONFIG` か `engawa.json[assets]` で差し替え可（`views._asset_path`・ADR-0010 を背景にも拡張）。残: 表情追加（ウインク/耳ピン/びっくり）。背景の**昼夜は tint 層で実装済み**（adr/0028・下記）。
  - [x] 差し替え機構＋state→アニメ配線: `sprite.json`（リポジトリ直下・config／env `ENGAWA_SPRITE_CONFIG`）＝frame_w/h・scale・animations{idle/blink/talk/listen/attentive: frames,fps}。Python が PNG を **data URI で HTML へ注入**（`build_web_html`）、JS が `chaState()`→コマ切り出しで blit（`imageSmoothing` 無効）。**シート無し/欠損は procedural にフォールバック**（コード不触で皮交換）。Python ユニット＋JS `node --check`（procedural/injected 両経路）＋全8スイート緑＋frameless 窓で実描画 GUI スモーク OK
  - [x] **placeholder ドット絵を生成・有効化**（6/27）: Pillow で 32x32×7コマ（idle×2/blink/talk×2/listen=耳ピン/attentive=目大）の茶々(猫)を生成→`chacha.png`（リポジトリ直下）＋`sprite.json` enabled:true で稼働。質は仮（再描画の下敷き用）。生成器は scratchpad の gen_sprite.py
  - [x] 表示サイズ・位置を自動算出（6/27）: スプライト使用時は `#cha` の表示寸法/位置を scene 高さと frame_w/h から JS が計算（`scale` は収まる範囲で整数クランプ＝省略で最大フィット・内部は等倍でクリスプ）。どんなコマ寸法のシートを置いても CSS を毎回いじらず収まる
  - [x] 本番の絵に差し替え（6/28・Gemini 三毛猫）= ユーザーが Gemini で「猫だけ・座布団なし・グレー背景」の8コマアニメ列を生成 → 私が grayキー抜き→高さ正規化→64高に整列して 8コマシート化、さらに「コマ0由来の目/口/耳差分」2コマ(8,9)を自作追加して **`chacha.png`(80×64×**10**＝800×64)** に、`sprite.json` で state→コマ割当。抜き手順は scratchpad の extract_seq.py（gray proximity key・市松焼込みは色キー・座布団付き版は flood不可だった等の知見あり）。Aseprite 購入不要だった
    - ⚠ AI の8コマは「連続アニメ」でなく「別々の表情ポートレート」＝コマ間が不連続でループするとぴくつく。対策で**各 state を単一コマ＋CSS呼吸(scaleY・座布団無いので浮かない)**に。割当: idle/blink=0, talk/attentive=2(笑顔), listen=3(見上げ)。fast 切替(2↔7)はやめた
    - [ ] （任意）滑らかなまばたき: 「コマ0の目だけ閉じた版」を私が自作すれば顔を変えず目パチが足せる（procedural の手法を実コマに適用）
  - [x] 背景の昼夜（adr/0028・7/4）＝**A案 tint 層で実装**。当初は B案（時間帯別 `scene.png` を `tod` で切替）構想だったが、2Dゲーの世界標準解＝「画像差し替えでなく1枚の色膜を lerp」へ方向転換。`daynight.layers(now)`（純関数・大阪時刻→`{tint,glow,lamp}`）を poll が毎回配り、#scene に膜3枚（`#tint` 乗算＋`#glow` 加算月光＋`#lamp` 障子ごしの暖色室内灯・`isolation:isolate` で窓外に漏らさない）を JS が適用。昼=白(無変化)/夕=桃/夜=青灰＋隅の月明かり＋夜は部屋から灯り。`ENGAWA_DAYNIGHT=0`(既定1)で固定。実数値の起点は実時間癒しゲー Usagi Shima。**プレビュー `/daynight`**（デバッグ再生＝`/arc` と同筋・7/5）＝実時間だと夕→夜を見るのに待つので、`/daynight HH:MM`=時刻固定・`/daynight demo [from to secs]`=早送り再生（既定 16:00→22:00 を40秒→終わったら実時間へ自動復帰）・`/daynight auto`=プレビュー解除で実時間へ。**さらに `/daynight on|off`＝機能の有効無効を `engawa.json[ui].daynight` に保存**（ライブ反映・`/font save` 方式・7/5 ユーザー要望「移ろいを設定で制御したい」）。仮想時刻の解決は **clock を持たない純関数**（`parse_override`/`override_minute`/`effective_layers`／real now＋demo経過秒だけで判断）＝unittest 可。テスト: `test_daynight`（キーフレーム・全分の値域・連続性＋override 純関数＋on/off/auto/pin/demo の parse）＋`test_views`（poll に day 載る/無効で None/HTML に膜3枚＋applyDay／pin・auto・demo自動復帰・enabled トグルで poll ゲート・機能オフで無視）＋`test_scheduler`（`/daynight` 固定/早送り/実時間/不正/状態＋on\|off でライブ＆engawa.json 保存＋無効中プレビューは促す・茶々に流さない）。見た目は目視（`/daynight demo` で待たず確認可）。**行灯（障子ごしの室内灯 lamp）は 7/5 追加済み**（夕点灯→夜満・`#lamp` の暖色 screen・キーフレームに lamp チャンネル）。**残 (任意)**: 天気を tint に効かせる（曇りで彩度↓・adr/0012 と地続き）／室内灯を障子の桟の縦帯にする作り込み。B案は捨てず「特別な一枚」を `ENGAWA_SCENE_BG` で差せる。
  - [ ] （任意）天気/時刻 由来の mood も state に足す（今は talk/listen/attentive/blink/idle）
  - [ ] **【大物・構想】庭側ビュー＝横パンで季節感あふれる日本庭園（7/5 ユーザー着想「横スクロールすると季節感あふれる日本庭園が見える感じｗ」）**
    - **思想的に地続き**: 縁側は「部屋（障子）と庭のあいだ」の境界。今のシーンは障子＋板の間＝**部屋側だけ**で、庭は概念が最初から含む"もう半分"＝増築でなく空間の完成。
    - **庭はもう住人がいる**: 客人(Codex)は「画面外＝庭側」にいる扱い（ADR-0009/Inc4・`#kehai` の葉そよぎ＋茶々の耳ピンだけで表現）。庭ビューができれば**横パンで客人が庭の小径を歩いてくる来訪路**に literally 舞台が出来る。箱庭アークの雀/猫/風(ADR-0011)も庭が本当の住処に。
    - **実現の芯＝背景の工夫**（ユーザー見立て「背景工夫したらできるかなぁ」）: `scene` を横に広い一枚にして**窓内でパン**（部屋↔庭をドラッグで横移動＝小窓のまま覗き込む。窓拡大より「隅にそっと居る」気配を壊さない・要検討＝設計の分かれ目）。昼夜 tint／室内灯（ADR-0028）＋天気オーバーレイ(ADR-0012)は**庭にもそのまま乗る**（夕暮れの庭・雨の庭・雪の朝）。
    - **季節感＝環境反応の3本目の軸**: 実大阪の天気(ADR-0012)＋実時刻の昼夜(ADR-0028)に続き、**季節を実日付から**（桜/新緑/紅葉/雪）。新機構でなく既存思想の延長＝ADR-0019「意味 state＋config 駆動の差し替えアセット」の枠に収まる。
    - **木の葉が揺れる**（ユーザー要望）: 庭の常時アンビエントな葉揺れ＝来訪の `#kehai` ドリフトや呼吸 `breathe` と同じ CSS アニメの手（気配の一過性ドリフトとは別に、常時ゆらぐ層）。風アーク(ADR-0011)と連動させると強弱も付く。
    - **位置づけ**: P5 presentation の大物。ADR-0010(皮)／0019(意味state+差し替え)／0028(昼夜tint)／0012(実天気)／0011(箱庭アーク) と全部地続き。急がない・実装はずっと先。着手時は「窓内パン vs 窓拡大」を先に決める（ADR 案件）。
    - **最終形＝縁側の canvas シーン化（7/18 ユーザー着想「ゲームの画面にすれば自由度上がる」）**: 背景/小物/茶々/粒子/昼夜を1枚の canvas 描画ループで合成（茶々は既に canvas）。雨雪・蛍・横パン（カメラ）が同じループの住人になる。描画エンジン化≠ゲーム化（静けさの芯は不変・原則#2）。**props 台帳（ADR-0032）は renderer 非依存で先行**＝シーン化の日に台帳ごと引っ越す。
  - [~] **【Inc1 実装済み 7/18・ADR-0032】小物演出＝お茶/おにぎり等の生活小物（7/5 ユーザー着想「お茶とかおにぎりの小物演出／生成AIならではで自由度増やしたい」）**
    - [x] **Inc1: props レイヤ＋蚊取り線香（7/18・ADR-0032）**＝台帳（`assets/props.json`）駆動のオーバーレイ層。月ゲート（蚊取り線香=6〜9月＝実日付の季節反応）・汎用演出 `rise`（煙の粒＝♥と同機構・**小物専用コード禁止＝新小物はコード0行**）・皮の差し替え口（`ENGAWA_PROPS_CONFIG`）・昼夜膜の下（夜は小物も暮れる）。素材はユーザー持ち込みの Gemini 画像から煙を成分分解で除去（`assets/props/katori-senko.png`）。テスト12件＝463 green・JS node --check OK。**位置の目視調整はユーザー**（props.json の left_pct/bottom_pct）。
    - [x] **v2 化＋茶々が知る（7/18 同日・「増える前提で設計工夫を」のユーザー要請）**: 台帳を entity+component（place/when/effect/narrate）へ・条件は述語 Registry（全AND・未知フィールド無視＝前方互換）・**資産は起動時注入/表示集合は poll 配信**（Flyweight+Observer＝月替わり再起動不要・Inc2 の点け消しの通り道）・`narrate` が環境行に合流（「縁側には**あなたが焚いた**蚊取り線香がある」＝フィクションで主のエージェンシーを立てる）。468 green・JS node --check OK。
    - [ ] **Inc2 本命＝②点火イベント（茶々が焚く「ように見える」）**: ゲート遷移を Scheduler が検知→「線香点けよか」の一言→出現。active 集合の正本が Scheduler 状態に移り View ポート拡張が要る（規模=A の約2倍・ADR-0032 備考に設計）。その他: 時間帯ゲート（述語1個）／お茶・おにぎりのライブラリ拡充。
    - 妥協メモ（7/18）: 446px 素材→46px 表示の縮小ノイズは**現状で妥協**（ユーザー判断「もうこれでいいや」）。低ドット数の Gemini 再生成は不発。気になったら**機械縮小**（Pillow NEAREST/BOX で事前 46px 化＝素材1px=表示1px・スクリプト設計済み）を試す。
    - **狙い**: 茶々の横にお茶、昼下がりにおにぎり、暑い日は冷やし…＝縁側が"生活の場"に。環境反応の核と地続き（時刻/天気/アークに小物が乗る）。
    - **構造**: 小物＝#scene の追加レイヤ（背景/スプライト/昼夜膜/葉の気配 と同じ列）。意味 state で出し入れ＝ADR-0019/0010（皮）＋ADR-0014（種注入）と同型。
    - **生成AIの活かし方（3案）**:
      - ① **Gemini で小物ライブラリ＋LLM が選ぶ（推奨の出発点）**: scene/chacha を作ったのと同じ手で同じピクセル画風の小物を事前生成→`assets/props/`。どれを・いつ出すかは茶々(LLM)が決める（「お茶にするわ」→prop:tea・種注入と同じ経路）。画風統一・実行時コスト0・サブスク枠内（ADR-0002）。自由度＝ライブラリが育つ＋LLM 選択。
      - ② **絵文字/テキスト小物（最大自由・ゼロコスト・試作向き）**: 茶々が任意の絵文字を吐く→🍵🍙📖 が横にふわっと。literally 無限＝真の生成・追加依存0。ただしピクセル画風とは浮く。
      - ③ **実行時画像生成（"生成"文字通り・重い）**: クラウド画像 API＝従量課金で原則#1 抵触。ローカル SD（RTX2070S 8GB で SD1.5 可）は課金無しだが重い/数秒/画風を LoRA で縛らないと縁側から浮く＝「動く実験」枠（ローカル LLM backend と同じ立ち位置）。
    - **設計の分かれ目**: トリガー（時刻/天気/アーク/茶々の LLM タグ）・置き場（板の間の茶々の隣）・アークと絡めるか（"お茶タイム"アーク＝小物＋茶々の一言）。
  - [ ] **【構想・検討中】客人スキル＝絵描きが今日の候を一枚描き残す（7/11 ユーザー着想「絵描きが来たら本当に絵を描いて欲しい」）**
    - **狙い**: 絵描きの人格を着た客人（Codex）が来訪の折、いま時分の**七十二候**（`sources.KOU`・実装済み）に合わせて墨絵/俳画の一枚をさらっと描き残す。雀アークが今日の空に反応するように、絵は今日の候に反応する＝**環境反応の核と地続き**。上の「小物演出」と同じ presentation チャネル族（ADR-0019・意味 state＋差し替えアセット）で、こちらは「客人の能力＝スキル」という切り口。
    - **「客人スキル」の構造（2つの半分）**: ①プロンプト側＝絵描き persona の注入文に「一枚描き残せる」を足す（persona は召喚時注入＝ADR-0008 の延長）。②アプリ側＝出力を捕まえて描画する presentation チャネル。**guest 出力の捕捉は既存の `room_speakers.py`（`is_error_payload` で API エラーを畳む前例）に「描くビートなら presentation へ回す」を一点足す**のが素直＝新モジュール不要。
    - **YAGNI**: スキルは今「描く」1個だけ＝**汎用 framework/registry は作らない**。ゲームが2個目で registry 化した流儀（ADR-0017）どおり、1個目は具体で作り切る。行商人「品を見せる」・風流人「短冊に一句」等の2個目が来たら「客人スキル registry」を抽出。
    - **生成の段階（小物演出の3案に対応＋墨絵に特化した第2案）**: ① Gemini 事前生成ライブラリ＝"描く"には不向き（既製の使い回しになる）。②' **procedural 墨絵（推奨の出発点）**＝コードが seed 乱数で**毎回ちがう一枚**を生成（固定画像でない・AI画像モデル無し・課金0・トークン安）。テーパー筆＋朱の落款、余白主役のミニマルで様式的に成立。③ ローカル SD＝"本物の AI が描く"・写実/画風自由だが重い（GPU・数秒・LoRA で画風を縛らないと縁側から浮く）。
    - **⚠️ 誰が絵を決めるか（ADR-0014 の教訓）**: 客人 LLM に**正確な spec/JSON を吐かせるのは不安定**（種を 0/10 で無視した実績）。→ **アプリ駆動**が本命＝「何を（motif）・いつ」は AI（絵描きが居る＋描くビート＋flavor の一言）、「どう描くか（筆致）」は procedural コード、の分業。②' はこれと噛み合う。
    - **プロトタイプ実証済み（7/11・会話中アーティファクト）**: procedural 墨絵4枚（桃始笑/温風至/玄鳥去/梅子黄）＋「↻ 描き直し」で同じ候が毎回別構図に生成されるのを確認＝「決まった絵の使い回しでない」を可視化。画像モデル・外部素材ゼロで縁側の静けさを壊さない手触り。
    - **検討中の未決点**: (1) **②' procedural 墨絵 か ③ ローカル SD か**（軽さ・課金0・静けさ vs 写実・自由度・重さ）。(2) 発火条件（絵描き来訪時のみ／確率／描くビートの置き方）。(3) 画帳に溜めるか（残す/流す）。(4) 実装するなら ADR-0022(voice)でなく **ADR-0019(presentation)の子として ADR 起こし**。→ **ユーザーがもう少し検討**（急がない）。
    - **位置づけ**: P5 presentation。ADR-0019(意味state+差し替え)/0010(皮)/0014(種注入) と地続き＝庭ビューと並ぶ presentation の新章。まず①＋②(絵文字プロトタイプ)で「LLM が自由に小物を選ぶ」感触→③は欲が育ったら実験。
- [~] Increment 4: 客人来訪の演出（6/27 実装・GUI 確認はユーザー）＝**画面外＋気配方式**。当初の「障子に影」は *障子越しに会話するのは不自然*（障子=家の仕切り／客人=塀の向こう＝庭側）として却下→作り直し。客人は庭側＝画面外にいる扱いで姿は描かず、**到着時に庭先の葉がそよぐ気配**（`#kehai` のドリフト）＋**茶々の耳ピン反応**（既存 listen）＋会話ログで表す。信号は poll の guest voice 由来（`chacha.lastGuest` 12s 窓・立ち上がりで気配）＝Python 変更なし・sprite/procedural 両対応。JS `node --check`＋全8スイート緑
  - [ ] 実機の見た目確認（GUI）: `/codex <人格>` で庭先に葉のそよぎ＋茶々が耳ピン。手元検証はユーザー（Chrome 拡張未接続で自己描画確認は不可）
- [ ] **（構想）音をつける＝環境音＋茶々の鳴き声**（体験の厚み・環境反応の核と地続き）
  - **環境音**: 時刻/天気/アークに連動した BGS（朝の鳥、雨音、風、夕方のひぐらし 等）。`tod`・天気・アーク state を信号に web 側で鳴らす（背景の時間帯バージョンと同じ思想で state→音を配線）。
  - **茶々の鳴き声（音）**: 構った時に「にゃ」等の*音*（三毛猫なので猫声）。※視覚の反応（ダブルクリック→「ニャー」吹き出し＋ハート♥）は実装済み（7/5・`views.py` の `meow`/`hearts`）＝ここは*音*の追加。state（talk/listen/attentive）にも軽い反応音を当てられる。来訪・天気の移ろい・アーク起承転結にも効果音の余地。
  - **設計の筋**: 音アセットは差し替え可能な config 主導（`sprite.json`/`topic_sources.json` と同じ流儀＝`sound.json` 等でファイル/音量/有効を宣言・コードに埋めない）。**既定ミュート＋音量つまみ**（`engawa.json`）で常駐アプリでも邪魔しない（「窓を汚さない・明示操作」の方針と整合）。web は HTML5 Audio、console は無音。

## ゲーム（ADR-0017・AIが既存ゲームに参加）
> 「ゲームは自作せず既存実装に AI が参加」。Pyxel/pygame はUI総取替で不適と判断、RLCard（読める状態＋合法手）を Port&Adapter で。
- [x] **Inc1**: Game ポート核 `game.py`（GameAdapter/Player/GameSession 人数非依存/レジストリ・依存ゼロ・FakeGame でテスト）
- [x] **Inc2**: `game_rlcard.py`（RLCardAdapter・**rlcard 依存はここだけ**）＋step方式＋AI-only観戦。実 rlcard で3人BJ完走を隔離venvで検証。rlcard は**任意依存**（無くてもコア app/テストは動く・adapter テストは skip）
- [x] **Inc3**: 実 LLM プレイヤー（茶々=resident・state＋合法手→手）＋`/blackjack [見る]`＋console＋tickペース。**A方式＝基本 私＋茶々（観戦は茶々のみ）で codex 不要**、ゲームが人数を要求する時だけ客人(codex)で埋めて終局で破棄。手番プロンプトに自分のスロットを明示（全員の手札が見える blackjack 対策）。配線は FakeGame＋fake codex で全116 PASS
  - [ ] **実機 E2E（ユーザー）**: `pip install rlcard` → `/blackjack 見る`（茶々がディーラーと・codex不要）/`/blackjack`（私+茶々）。**実 claude が合法手(hit/stand)をちゃんと選ぶか**・パース外し時のフォールバック頻度・テンポ
- [x] **Inc4a**: 観戦表示を View ポート化（game_open/update/close）＋構造化スナップショット。console はテキスト維持。BlackjackRender.snapshot。
- [x] **Inc4b**: **観戦窓（別ウィンドウ・カード描画）**。WebView が対局開始で第2窓(GAME_HTML・緑フェルトの札卓)を本窓の隣に生成→snapshot を poll してカード描画→終局で閉じる。作れない環境は本窓ログへフォールバック。JS は node --check OK。
  - [ ] **実機の見た目確認（ユーザー）**: web 起動→`/blackjack 見る` で隣に札卓窓が出てカードが見えるか／位置・サイズ感／×で閉じるか
  - [x] **本窓×で観戦窓が残る不具合（実装6/29・ユーザー報告／実機OK 6/29）**＝本窓の×（`WebView.close()`）が本窓だけ destroy し観戦窓(第2窓)を残すと、`webview.start()` が返らず scheduler の teardown（finally の `view.game_close()`）に入れない＝観戦窓が残りプロセスも生き続ける。`close()` 冒頭で `game_close()` を呼び**両窓を畳んでから**本窓を閉じるよう修正。テスト `test_views.TestWebViewCloseClosesGameWindow`(2件)。**実機確認: 本体終了と同時に観戦窓も閉じるのを確認（ユーザー 6/29）**。
  - [x] **観戦窓×で「ゲームモードのまま復帰不能」になる不具合（実装6/29・ユーザー報告）**＝観戦窓の×（`_GameApi.close`）が `view.game_close()` で**窓を destroy するだけ**で `Scheduler.game` を残していた＝以後ずっと対局中扱い（平文入力は全部「手」・縁側に戻れず `/quit` でアプリごと閉じるしかない）。観戦窓×を **`WebView.request_game_abort()`**（窓を閉じる＋入力チャネル `_inq` に制御トークン `views.GAME_CLOSE_REQUEST` を積む・スレッド安全）に変更し、scheduler は `on_user_input` 冒頭でそれを受けて **`_abort_game`（お開き＝state クリア＋客人破棄＋観戦窓クローズ→縁側へ）**。`_abort_game_on_timeout` と共通の `_teardown_game` に整理。テスト `test_scheduler`(対局中×でお開き／非対局時 no-op)＋`test_views.TestGameWindowAbort`(窓を閉じ合図を積む)。
  - [x] **対局中に固まる別経路2つ（実装6/29・上記調査で判明）**＝①**tick ループの脆さ**: `_tick` の game ブロックを `try/except` で囲み、**timeout 以外の例外**（adapter 死亡=ConnectionError／rlcard 不正状態 等）も `_abort_game_on_error` でお開きに（例外が `_tick` を抜けて `_tick_loop` を殺す＝永久停止を防ぐ）。②**`/codex` の game ガード**: `_summon_guest` 冒頭で対局中なら「今は対局中や」で弾く（room と game の同時成立を防ぐ）。テスト: `test_scheduler` に異常系（AI手番の例外でお開き／adapter.play 例外でお開き／対局中 /codex 拒否）＋正常系（非対局時 /codex は通る）の4件。全150 PASS。
- [ ] （任意）観戦窓に手番リアクション台詞・対局時の hit/stand ボタン（今は本窓でテキスト入力）／pixel-art カード化
- [x] **UNO/レダックポーカーを起動可能に（6/29）**＝アダプタは元から登録済み（`game_rlcard.py`）だったが起動コマンドが `/blackjack` しか無かった。**汎用 `/game <id> [見る]`** を追加（`/blackjack`/`/bj` は別名で維持）。空/不明 id は遊べる一覧を出す。観戦/参加の人数は登録メタ `(min,max)` に**クランプ**（leduc 等2人最少のゲームを観戦 want=1 で壊さない）。UNO/leduc は**カード描画(render)未対応＝観戦窓は move をテキスト表示**（GAME_HTML の `renderText` フォールバック）／console は本窓テキスト。LLM プレイヤーの注入・手パースはゲーム非依存なのでそのまま動く。実 rlcard スモークで uno/leduc/blackjack の `legal_moves` 取得を確認。テスト `test_scheduler.TestGameMode`(+5件・/game uno・不明 id 一覧・id 省略・/blackjack 別名・人数クランプ)。**残: 実 LLM が UNO の手(色-数/ワイルド)をちゃんと選ぶかの実機 E2E（ユーザー）**。
  - [ ] （任意）UNO/leduc の**カード/盤面 render**（観戦窓を blackjack 同様に絵で）／**PettingZoo アダプタ**（盤ゲーム＝新規 `game_pettingzoo.py`・AEC/action mask・任意依存追加・三目/connect4 が軽い）／手番のリアクション台詞
- [ ] **（設計オープン）対局中に茶々/客人へ話しかける（人→AI 雑談チャネル）**＝今は対局中の平文入力は**全部「手」**として解釈され、雑談する口が無い（ADR-0017）。やるなら**ユーザーに `/say` を覚えさせず、対局中だけ内部で「手か雑談か」を仕分ける**方向（自分の番×合法手→手／それ以外→茶々への雑談。今の拒否メッセージ「その手は出せん」「他のプレイヤーの番」が雑談に化ける）。
  - **本当の難所は仕分けでなく並行性**: 茶々は同じ長命セッションで手も選ぶ。tick が裏で `茶々.prompt(手番)` を回す所へ雑談 `茶々.prompt` を被せると**同一 ACP セッションに prompt 2本同時＝チャンク混線/pending 取り違え**で壊れる。雑談注入をゲーム手番進行と**直列化**（`drive_lock` で囲む等・召喚/対局開始と同じ流儀）が必須要件。
  - 候補と論点（**まだ決め打ちしない＝もっと良い設計の余地あり**）: ①内部仕分け（move-first・else 雑談／打ち損じが雑談化する軽微な弊害）②明示 `/say <text>`（堅いが口を覚える必要）③web の宛先チップで茶々宛を雑談へ（web 限定）。客人は席埋めゲームでしか居ないのでまず茶々だけが現実的。並行性の直列化はどの案でも共通で要る。
## 多言語・多方言（voice バンドル・ADR-0022）

茶々の「声」を **voice 単位**（`ja-osaka`/`ja-kyoto`/`ja-kagoshima`/`en`…）で差し替え。**base 言語 ⟂ voice を分離**し継承（`<voice>→<base>→既定`）。中身（各地域の声）は現地が書き起こす＝transcreation（機械翻訳しない・原則#2）。設計は ADR-0022。~~まず方言ユースケースで継ぎ目を安く検証してから言語へ~~＝**実際は英語（言語）の実需が先に来て Inc1/Inc2 を 7/18 実装**（方言バンドルは未同梱・persona 一枚差しの継ぎ目は `test_voice.test_persona_only_bundle` で検証済み＝京都弁等はファイルを足すだけ）。

- [x] **Inc1: voice 選択＋persona オーバーレイ**＝**実装済み（7/18）**。`src/voice.py`（解決=env `ENGAWA_VOICE`>json>既定 `ja-osaka` 組み込み・`voices/<id>/` の meta/persona/strings・base 継承・欠損は組み込みへフォールバック＝消せば現状維持）。注入は `acp.setup_persona_dir`／`agent_openai` の system が `voice.persona_text()` を使う（両 backend 同文・ADR-0026）。起動行に `声=<label>`（既定時は非表示）。ユニット test_voice 6件。
- [x] **Inc2: UI シェルの i18n（英語向け）**＝**実装済み（7/18）**。`voice.loc(key, 日本語既定)` で高頻度シェルを鍵化（/help・barge-in 演出・来訪・中座・timeout・起動行・web 固定ラベル=`views._localize_html`）＋`prompts._lang_note()`（llm_lang 時のみ言語指示1行・JP では不変）。**同梱 `voices/en`**（英語の茶々=transcreation＋strings 訳＋llm_lang=en）・spec datas 同梱・`engawa.json.sample[voice]`。配線テスト test_voice 7件（実 en バンドル検証込み）。
  - [ ] **未鍵化の後送り**（日本語フォールバックで動く・要望が出たら鍵化）: /model 詳細・/restart 経路・/arc /game の対話文言・commands.py（/font /daynight）・game_controller。~~住人表示名「茶々→Chacha」切替は culture/voice の判断待ち（固有名として維持中）~~＝**切替済み（7/19 ユーザー判断）**: チップ=Chacha・話者タグ=茶々の画面内不一致が実機で露見→`voice.resident_name()`（strings `resident_name`・既定「茶々」）を新設し scheduler（ソロ turn_start/中座 say）・room_speakers（room 話者タグ）・game_controller（対局プレイヤー名）・ConsoleView（who 追従化）で使用。宛先解決は文面ベース（茶々/Chacha 両対応済み）で無影響。**併せて宛先バー見出し「宛先」の鍵化漏れも修正**（`ui_addr`=To・`views._localize_html`）。**→v0.1.9 直後の実機で2バグ露見・即日補修（7/19）**: (a) 注入/表示の一人二役＝room の LLM 窓まで `Chacha「…」` になり茶々の口調が客人化→`Speaker.display` 分離（窓は「茶々」に復帰） (b) web JS の「茶々」ハードコード3箇所（ソロ転写ラベル/say 色分け/客人在室判定）→`const RESIDENT` 注入に統一。詳細 adr/0022 追補。`/codex` 空引数の既定 persona「気まぐれな旅の客」は日本語のまま。**console の固定ヘッダも後送り**（`客人 ›`・`縁側の外`・`（移ろい）`・`_voice_block` の客人ラベル＝`views.py` ConsoleView・codex 7/19 [中]2＝en 常用は web で console は副 UI・鍵化はローカライズ再設計の台帳でまとめて。非日本語 Windows の console では豆腐/パイプ時 encode エラーの可能性あり＝7/19 検討）。起動 tag のラベル（茶々=/既定/声=）は**鍵化済み（7/19・codex [中]1 反映＝`ui_model_default`/`ui_voice_label`）**。
  - [x] **英語運用の同類穴3件を修正（7/18・README 点検で発見→即日）**: ① `resolve_addressee` に英字対応（`_LATIN_RUN`+機能語ストップ+単語境界・「Guest/Chacha/both/you two」も解決＝英語 persona の客人を名前で呼べる） ② **`strip_resident_leak` の思考除去（先頭の非日本語塊=染み出し）を非日本語 voice では跳ばす**＝英文中の和語引用で正当な英文が消える破壊を防止（`jp=` 引数・既定は `voice.llm_lang` から導出） ③ `/arc` に英別名 sparrow/cat/wind（`scheduler.ARC_KEY_ALIASES`・source key は和字のまま）。テスト10件＝447 green。問題なしを確認: ゲーム手入力（`parse_move`=rlcard 英語トークン照合）・`is_error_payload`（構造検出）。トピックの persona タグ和字マッチは英語客人に汎用ネタだけ流れる＝壊れない（culture Inc3 で吸収余地）。
  - [x] **実機E2E（ユーザー）**＝**済（7/18・「いい感じ」）**: `ENGAWA_VOICE=en`（engawa-en.bat）で英語の茶々のトーン・UI 訳を目視確認。
  - [x] **【バグ・実測済み→修正済み 7/19】ソロ経路（ambient/arc/transition）に `_lang_note()` が無い＝en モードの起動直後〜初話しかけ前の独り言が日本語になる（7/19 発見）**＝**`voice.lang_note()` へ移設＋sources 3ビルダー後置で修正・再測 12/12 英語（ambient/arc×3×両エンジン・qwen も人格維持）・474 green・codex diff レビュー2件反映（probe の trial ごと新品セッション化＋JP 1バイト不変のバイト一致検証・`codex/review-2026-07-19-diff-lang-note.md`）・詳細は adr/0022 追記**。以下は発見時の記録:＝lang note は `prompts.py` のビルダー（user_narration/room_*）にしか無く、`sources.py` 側の narration（ambient_narration/event_narration/transition_narration）は言語指示ゼロ。**測定ハーネス（10回×4カテゴリ×2エンジン=80発話・実 Claude opus＋qwen3.5-9b）で二値と実証**: lang note 有り経路=40/40 きれいな英語（日本語片・訳語ゆれゼロ・蚊取り線香 narrate も自然に消化）／無し経路=非「……」発話が両エンジンほぼ100%日本語（Claude ambient 7/7・arc 10/10、qwen 20/20。qwen は日本語落ち時に人格も崩壊=女言葉丁寧語）。英語 persona は「英語で書かれている」だけで言語を縛らない、が結論。7/18 E2E で見えなかったのは①実装がモジュール境界（prompts.py のみ）でスイープされ概念単位（住人に届く全注入）でされなかった ②層またぎ不変条件の合成テスト不在（ADR-0031 ARRIVE 穴と同型） ③E2E が「先に話しかけ→英語慣性」の順序で露出窓（起動→idle→初手つぶやき）を踏まなかった、の3枚重ね。**修正案**: `lang_note` を `voice.py` へ移設（prompts は委譲・sources→voice は葉 import で一方向維持）し sources 3ビルダー末尾へ後置＋「en voice で住人向け注入ビルダー全列挙に指示行が載る」合成テスト。JP voice では空文字=1バイト不変。WEATHER_CODE 英語列は測定の結果**不要と確定**（茶々は天気を復唱しない設計ゆえ訳語ゆれ自体が発生しない）。
  - [x] **【運用・7/19 ユーザー方針→同日実装】部分修正の検証を「部分だけ見てOK」で終わらせない＝E2E で他経路の破れを検出できる常設層を持つ**＝**層A=`tests/test_injection_lang.py`（列挙×不変条件＋命名 canary）・層B=`tests/e2e/leak_probe.py`（opt-in 実 LLM 一巡・新品セッション仕様・discover 非対象＝skipped 基準不変）実装済み。層C（初手順序を変える窓）は probe docstring と adr/0022 追記に明文化**。以下は方針の記録:＝上記バグの再発防止を一般化。三層で受ける: **層A（CI・fake agent）**=注入経路ビルダーの列挙×不変条件（lang note/ガード類）の合成テスト＝毎 push 機械検証／**層B（opt-in・実 LLM）**=今回の測定ハーネス `leak_test.py`（実ビルダー×実 agent×新品セッション×カテゴリ網羅）を repo に常設昇格（置き場は tests/e2e/ か poc/・実行は課金ありのため opt-in=ENGAWA_E2E 方式・test green baseline の skipped opt-in 前例に倣う）＝部分修正後に全注入経路を実 LLM で一巡できるスモーク／**層C（人手・目視）**=E2E シナリオに「初手順序を変える」窓（起動→idle 放置→初手つぶやき／起動→即話しかけ）を明文化。ハーネスの現物は scratchpad（セッション消滅前に昇格判断）。
  - [ ] **茶々の she/her を日本語版と揃えて中立にできるか検討（7/19 ユーザー提起）**＝日本語版は代名詞を使わない（「茶々」で通す）が、英語化で暗黙に **she/her に確定**していた。出現は `voices/en/strings.json` の **3キーのみ**（`help_restart`「her output」/`resident_hiccup`「give her a beat」/`absence_away`「calling her back」・`persona.md` は代名詞なし）＝局所。検討の骨: ①名前リピート（"Chacha's output" / "give Chacha a moment" / "calling Chacha back"＝日本語版と同じ流儀・3箇所なら不自然にならないか） ②they（英語として中立だが「一匹の猫」に they は座りの確認要） ③文ごと組み替えて代名詞回避 ④現状維持（三毛猫=calico はほぼ雌という文化連想があり she は自然、という擁護）。**LLM 出力側が自称/地の文で性別を言い出さないかも一緒に見る**（persona に明示指定なし＝揺れうる）。strings 差し替えだけならコード0行・transcreation の質判断はユーザー目視。
- [ ] **【次章・7/19 ユーザー方針】ローカライズ再設計＝「現地の人がコード0行で、自分の土地の表記と自分の茶々を作りきれる」**（順序確定: ~~v0.1.9 を締め→ADR 起こし~~＝**v0.1.10 で締め・ADR-0033 起こし済み（7/19）**→次は**設計**→レビュー（codex 含む）→実装。決定の正本は `docs/adr/0033-localization-author-sovereignty-and-enforcement.md`＝著者主権原則・2種境界・単一台帳→雛形/掃引/lint・culture 接続・gettext 却下・未決5点は ADR 備考）。背景: opt-in 鍵化（日本語直書き＋包んだ所だけ loc）は「漏れ」が既定で、7/18〜7/19 に修正漏れが続発（同類穴3件・lang note ソロ経路・宛先・表示名・起動tag・console ヘッダ）＝発見手段がスクショ/実測/目視に依存。骨子: ①可視文字列は**すべて bundle から上書き可能**を原則化（ハードコードは著者が直せない壁） ②**単一台帳（strings registry）**＝キー＋日本語既定の一元化→`voices/_template/strings.json` 雛形を生成同梱（著者がキー一覧を知れる） ③同じ台帳から**掃引テスト**（開発者向け＝en で全 UI サーフェス render→JP 残存で red・EXEMPT は理由付き）と **bundle lint**（著者向け＝未訳キー一覧レポート・graceful fallback の自覚不能を解消） ④culture.json（Inc3）は同原則のデータ側（役名プール・地名・季節語彙）として接続。gettext は不採用方針（voice は言語でなく「声」が主役・loc が既に相当・ADR で却下理由明文化）。ADR-0033 予定。**テスト側の対策3本は 7/19 に前倒し実装済み**（「追記だけでは意味がない」ユーザー指摘→即日）: ①注入プロンプトのゴールデンスナップショット（`tests/test_prompt_snapshots.py`＋`tests/snapshots/`＝JP/en×6ビルダー・意図した変更は `ENGAWA_UPDATE_SNAPSHOTS=1` で再生成＝意図せぬプロンプト変更を diff で迫る） ②leak_probe の room 窓を本番配線化（RoomSpeakerFactory の実 Speaker 名＝fixture の配線迂回を塞ぐ） ③ブラウザテストに en 転写 DOM ラベル掃引ケース（`test_web_behavior`・skipped 基準は 1→2 に更新）。残りの掃引（全 UI サーフェス×EXEMPT 台帳）と著者側ツール（雛形生成・bundle lint）が ADR-0033 本体。codex 指摘の残件（起動tag[中]1・console ヘッダ[中]2・ADR 書式[低]＝`codex/review-2026-07-19-localization.md`）の扱いは v0.1.9 締め時に判断。（~~YAGNI・最初の外国語ロケールが来たら~~＝**en 到来で着手条件は成立（7/18）**。ただし英語運用でも時節 local トピックは回る＝「季節ネタ・客人ペルソナが日本語風のまま」が実際に痛くなってから着手＝もう一段の実需待ち→**7/19 に一段目の実需が露見**: en 画面で自発来訪の役名「気まぐれな旅の行商人 ›」が和字のまま話者タグに出る（ユーザー実機スクショ）＝`sources.GUEST_PERSONAS` の英語化は Inc3 の初手候補（ユーザー判断で Inc3 送り・7/19）。ほか qwen で PLACE_LABEL「大阪」の固有名詞素通し 2/40 も観測＝`ENGAWA_PLACE_LABEL=Osaka` の設定で解消可・地名/語彙の吸収も Inc3 と地続き）
  - 季節モデル（二十四節気/旬→相応）・天気語彙・客人ペルソナを voice/base 継承で差し替え。下記の天気負債を吸収。
- 関連負債の合流先: ~~「茶々用 CLAUDE.md を persona/ 別ディレクトリ運用」＝`voices/<id>/persona.md` に化ける~~＝**化けた（7/18・`voice.persona_text()` が両 backend へ注入・組み込み底は persona.py）** ／ ~~「天気座標の大阪固定→設定化」＝`culture.json`(Inc3)~~＝**単独 config 節 `[weather]` で先行実装済み（2026-07-11・上記 [x]）。culture.json が来たら地名/語彙ごと吸収する余地は残る**。
- スプライト（三毛猫）は言語中立＝不変（P5 と独立・ADR-0010/0019）。

## Open Questions（spec §15）
- [~] 長命セッションの compaction 戦略 / fork 閾値（Naraku の外部状態方式を流用できるか）
  - **染み出し不具合（7/3 実機報告）**＝長時間稼働で茶々ソロ出力に「注入プロンプトの復唱＋地の思考(英語/メタ)＋本物の台詞」が全部混じる（内部 compaction で「茶々として答えるだけ」の枠を失う筋）。Engawa は `agent_message_chunk` しか描画してない＝**モデルが本文チャンクとして吐いている**＝描画フィルタでなくモデル出力側の崩れ。
  - **対症（実装済み 7/3・commit 0e1ffe0）**: ①表示ガード `prompts.strip_resident_leak`（注入文の復唱＋先頭の思考を表示前に純関数で除去・原因を問わず効く・`ENGAWA_RESIDENT_GUARD` 既定1・ソロは一括描画に）＋②`/restart`（住人セッションを張り直し＝timeout 段階回復と同じ respawn 経路を共用）。テスト: `test_prompts`(8) ＋ `test_scheduler.TestRestartAndGuard`(5)。
  - **根治（実装済み 7/3・ADR-0027）**: ③茶々の「中座」＝世界観に溶かした定期セッション更新。住人ソロ発話が `absence.after_turns`(既定30＋ゆらぎ)たまったら次の idle で中座に入り、不在(`gap_sec` 既定18)の裏で `_restart_resident()`（黙って若返り）→ローカル定型で戻る。idle 限定（会話/来訪/対局中は行かない＝話の途中で忘れない）・`after_turns=0` でオフ。leave/return は LLM 非経由（`prompts.absence_leave/return`）。テスト: `test_scheduler.TestAbsenceRefresh`(8)。**発話トーン/頻度の体感は実機目視（engawa.json[absence] で調整）**。
  - **残**: 染み出し検知→自動再生成（1回崩れたターンを黙って引き直す）／room 側（`room_resident_prompt`）へのガード横展開／不在中の「空っぽの縁側」スプライト（P5 後日）。
- [ ] /codex <自由テキスト> のプロンプトインジェクション（配布時のみ要対策。検討メモ 6/27）
  - 脅威モデル: persona は `_codex_prompt` に直挿し＝注入面。ただし客人 codex も茶々もツール権限ゼロ（`acp.py`：fs/terminal=False・permission 即 cancelled）なので**最悪でも「変なテキストを吐く」だけ＝被害は体験/人格の崩れ**でセキュリティ被害ではない。守る本丸は機密でなく世界観・人格の保全
  - 注入は2段: `persona→codex→（『』で包んで）→茶々`。各ホップが面（codex が茶々を狙う2段目も）。`guest_narration` の `『…』` 枠付けは弱い緩和
  - 防御は多層（強い順）: ①封じ込め＝ツール無し・人間承認（ほぼ実装済み・王道）②入力を絞る＝**アローリスト persona**（自由入力はオプトイン化／注入面を原理的に消す・費用対効果最大）＋デリミタ＋「キャラ名として扱え」枠付け＋サニタイズ（長さ/改行/"無視して"拒否・脆い一次フィルタ）③評価で弾く＝LLM-as-judge/ガードレール分類器で persona や codex 出力を判定・カナリア。※評価は誤検知/回避ありの defense-in-depth で銀の弾丸ではない
  - 落とし所（配布時）: 既定アローリスト＋自由入力は枠付けオプトイン＋（やるなら）出力に軽い judge 1枚。完全防御は無い前提で比例した対策
- [ ] 客人の人格の作り込み度（環境イベント化なら厳密でなくてよい？）
- [ ] 「茶々が反応しない（……）」の UI 表現（既読スルー感）

## 技術的負債 / 要確認
- [x] **CI（GitHub Actions・7/4）**＝`.github/workflows/ci.yml` で push/PR に **tests**（Python 3.10–3.13 マトリクス・`python -m unittest discover`・依存不要＝`webview`/`rlcard` は遅延 import）＋ **ruff**（`ruff.toml`＝`select=F,E9` の実バグ級のみ・低ノイズ／`pylint` は日本語コメント/flat import 風にノイズ多で見送り）を自動実行。導入時 ruff 指摘2件（`prompts.py` の無意味 f-string・`test_debuglog` の未使用 import）を修正。README に CI バッジ。ローカルは `python -m ruff check src tests`。**追加(7/4): mermaid job**＝docs の `​```mermaid` 図を mmdc(`@mermaid-js/mermaid-cli`)で parse/render 検証（図が2回 GitHub でレンダー不能になった＝tests/ruff では拾えない再発防止・`--no-sandbox`・初回 run 緑）。**残（任意）: Windows ジョブ追加／`B`(bugbear) 等ルール拡張／pre-commit 化。**
- [x] **公開前レビュー（codex 2回目・7/4）織り込み**＝(1) 追跡 `.env.example`（実は legacy 専用の遺物・現行 `src/` は `.env` 非使用）が API キー入力を誘導＝方針と真逆 → **削除**。(2) `legacy/app.py`（旧・API 直叩き）に**実行ガード**（冒頭 `raise SystemExit`・原則#1・**後日 `legacy/` ごと削除**＝git 履歴＋ADR-0004 に温存）。(3) OpenAI backend は任意 URL/鍵を送れる穴 → **非ローカル endpoint を既定ブロック**（`agent_openai._ensure_endpoint_allowed`・`ENGAWA_OPENAI_ALLOW_REMOTE=1` で明示 opt-in・localhost/127/私設LAN/.local は許可）＋README/sample に「ローカル専用」明記。(4) 任意依存を `requirements-optional.txt`＋README の `pip install` で明示。テスト `test_agent_openai.TestOpenAIEndpointGuard`(3)。**残（ユーザー判断）: LICENSE 未設定（OSS化 or source-visible 明記）／ローカル `.env`（実キー入り・gitignore で未 push だが削除＋ローテ推奨）／`codex/AGENTS.md`（追跡・公開意図の確認）／Stop フックが Bash 前提（開発者向け注記）。**
- [x] **（設計・ADR-0026）Agent ポート抽出（7/4 実装）**＝`src/agent.py` に `Agent` Protocol＋中立 `AgentTimeoutError` を新設。`acp.ACPTimeoutError` は `AgentTimeoutError` を継承（型で正規化）、`AcpAgent` はその ACP 実装、`Scheduler` は `acp` を import せず `from agent import AgentTimeoutError` の中立例外だけ捕捉（scheduler のローカル変数 `agent` と衝突しないよう名前 import）。振る舞い不変。テスト `test_scheduler.TestAgentPort`(2)。
- [x] **（設計・ADR-0026）第2アダプタ `OpenAIAgent`（7/4 実装）**＝`src/agent_openai.py`＝OpenAI 互換 chat completions（LM Studio/Ollama）を Agent ポートで包む。API がステートレスなので**会話履歴を自前保持**（local 版の長命セッション・中座/close でリセット）。人格は `persona.py`（backend 中立に一元化＝ACP は CLAUDE.md／OpenAI は system で同じ文）。HTTP は stdlib urllib＋to_thread（**依存ゼロ**）、非ストリーミング（住人ガードが一括描画）、cancel は flag＋結果破棄（local は速い）、接続失敗/timeout は AgentTimeoutError で段階回復。`ENGAWA_RESIDENT_BACKEND=acp|openai`＋`ENGAWA_OPENAI_*`（composition root で spawn 切替・中座の再spawnも同factory）。テスト `test_agent_openai`(10)。**実機知見(7/4・Qwen3.5-9B on RTX2070S 8GB)**: 実 LLM で茶々が喋ることを確認（自発/話しかけ/履歴継続/染み出しゼロ・各~3.5s）。**Qwen3.5 は既定で推論(think)モードがオンで長考し、`reasoning_content` にだけ吐いて本文が空→タイムアウト**。`reasoning_effort:"none"` で解消（`chat_template_kwargs.enable_thinking:false` や `/no_think` はこの LM Studio ビルドでは効かず）＝`OpenAIAgent` に `reasoning`(既定 none)/`max_tokens` を追加。スモークは scratchpad `smoke_local_resident.py`（実 persona＋実注入文で GUI 無し実測）。**客人(codex)も backend 選択可（7/4・`ENGAWA_GUEST_BACKEND=acp|openai`・`OpenAIAgent.spawn_guest`）**＝人格は召喚時 prompt 注入なので system は汎用の客人枠(`GUEST_SYSTEM`)だけ・8GB 等では住人と同じロード済みモデルを共有（素の口調は似る/役と履歴は別）。実機（Qwen3.5-9B）で住人・客人とも在庫通り応答を確認（客人は役ごとに口調変化）。**残: トーン微調整（関西弁の揺れ・ネットスラング混入・「人間」を名前と誤認 等を persona/prompt で締めるかは目視判断）／客人ローカル時の window 重複（履歴＋prompt 内 window）は短い来訪では許容。**
  - **実機の質的評価(7/4)**: Qwen3.5-9B(8GB) は**配管はOKだが茶々の“穏やか関西弁×多ターン一貫性”には力不足**（自分の申し出を次ターンで否定・命令口調化・関西弁が崩れる＝GUI で会話が成り立たない体感）。**アダプタは完成＝モデル差し替えは config のみ**（`ENGAWA_OPENAI_MODEL`／`ENGAWA_OPENAI_BASE_URL`）。**既定は acp(Claude) のまま**で日常品質は不変。ローカルは「動く実験」として温存、**茶々用途にはより大きい/強いモデル**（or prompt/persona の短文・穏やか・few-shot 錨付け＋temperature 低め）が要る。評価は scratchpad `smoke_local_resident.py` で即再現可＝新モデルはこれで先に品質チェックしてから GUI へ。**当初 scripted smoke（`smoke_guest_visit.py`）では客人は実用寄りに見えたが、実 GUI の room では客人も破綻**（茶々の乱れた出力を受けて連鎖的に崩れる・「涼しげにきかんとおもう」「味がまろくできん」等＝台本入力が綺麗すぎた楽観だった）。**結論: 8GB の 9B は住人・客人とも現状は非現実的**＝既定 acp(Claude) 維持、OpenAIAgent は将来のより強い/大きいローカルモデルへの無改造差替口。**教訓: scripted smoke は実 room の GIGO を過小評価する＝最終判断は必ず GUI 実機で。**
- [ ] **【大物・ADR-0029】Scheduler を薄い Orchestrator に戻す**（責務過多＝839行/~31属性の God Object 化・Claude 分析＋Codex 独立提案＋Codex 第2R レビューで方針合意）。抽出順: ① CommandRouter（第一 PR＝`/font` `/daynight` だけ）→ ② `active` の意味分離（`active_source`/`active_guest`・Game 前・tick 条件も分ける）→ ③ GameController → ④ VisitController+RoomSpeakerFactory → ⑤ ResidentSessionManager（茶々発話は core `speak()`＋表示付き `inject_narration()` の2層）→ ⑥ Tick/Input を Chain of Responsibility に（最後）。罠: lock 所有は Scheduler 起点維持／`last_user_ts` を controller に散らさない／既存統合テストは緑のまま characterization、抽出とテスト再編を混ぜない。目安 839→250-350行。詳細と却下理由は **ADR-0029**、出自ブリーフは `codex/`。
    - [x] **Phase 1（CommandRouter・7/5・branch `refactor/adr-0029-command-router`）**＝`src/commands.py`（`Command`/`CommandRouter`/`CommandContext`＋`FontCommand`/`DayNightCommand`・verbatim 移設）。`Scheduler._command` は登録済みなら Router へ委譲・未登録はフォールバック。`_cmd_font`/`_cmd_daynight`＋不要 import 除去。**839→763 行**。既存統合テスト緑のまま＋`test_commands`(9)＝全 347 PASS・ruff clean。
    - [x] **Phase 2（`active` 意味分離・7/5・branch `refactor/adr-0029-active-split`）**＝`self.active`（arc/guest 兼用）を `active_source`（箱庭アーク）/`active_guest`（来訪 GuestSource）に分離。リネームだけでなく tick 条件（`active_mode`・`_next_interval`・`/arc` busy・`/codex` 畳み・shutdown visiting）も分割。`_conclude` は src が入ってる方を null に一般化。arc/guest は相互排他＝状態増やさず意味明確化。テストの `s.active` 8箇所も意味どおりリネーム（characterization 維持）。**全 347 PASS・ruff clean**。
    - [x] **Phase 3（GameController・7/5・branch `refactor/adr-0029-game-controller`）**＝対局の運用（`game`/`_game_*` state＋13メソッド）を `src/game_controller.py` の `GameController` へ verbatim 移設。Scheduler は `self.games` へ委譲（tick→on_tick／入力→on_user_input(bool)／/game→start／×→abort_by_user／shutdown→close）。Scheduler 状態への結び目は注入（preempt/bump_beat/resident_provider・drive_lock は所有のまま注入）。後方互換 `Scheduler.game`/`_make_game` プロパティで既存テスト維持。テストは `s.games.start` に向け直し＋`test_game_controller`(4・注入境界 isolation)。**scheduler.py 763→610 行**（累計 839→610・−229）。**全 351 PASS・ruff clean**。
    - [x] **Phase 4a（RoomSpeakerFactory・7/5・branch `refactor/adr-0029-room-speakers`）**＝最タングルの visit を安全に刻む第一歩。`_room_speakers` の Speaker クロージャ＋種プール＋timeout フラグを `src/room_speakers.py` の `RoomSpeakerFactory` へ verbatim 移設。茶々発話は Scheduler 注入の `_room_resident_speak`（turn_lock 下）＝**判断B の speak() seam を確定**（P5 で一本化）。room lifecycle は Scheduler に残す。テスト無改変＋`test_room_speakers`(6・isolation)＝**全 357 PASS・ruff clean**。scheduler.py 610→580。
    - [x] **Phase 5（speak 一本化・7/5・縮小版）**＝深い監査で full ResidentSessionManager は test ripple 大（module global patch／割り込み中核／注入~7）＝P4b と同じ「糊を注入で戻すだけ」と判明。判断B の payoff だけ回収＝共有コア `_speak_locked` に `_inject` と `_room_resident_speak` を集約（turn_lock は呼び側保持で barge-in 交錯を防ぐ）。新モジュール無し・テスト無改変で全 357 緑。scheduler 584。
    - [x] **ADR-0029 は P1〜4a＋speak で意図的に打ち切り（7/5）**＝Scheduler 839→584（−255）。自己完結サブシステム抽出＋speak 共通化は完了。残る P4b/P5full/P6 はコーディネーション糊の再配置＝Orchestrator の本業＝旨味薄・ripple 大で YAGNI。必要が育ったら再開（永続化/AbsencePolicy/N人会話の turn 管理 等）。詳細は ADR-0029「打ち切りの判断」。
  - [ ] （↑に吸収）View 入力を value 化（codexレビュー 7/4）＝観戦窓 close の `views.GAME_CLOSE_REQUEST` 特殊文字列・宛先 `"\x00<to>\x00<text>"` ワイヤ形式を `InputEvent(kind,text,to)` へ。Scheduler から `views` import を外す。CoR 化（⑥）や Input handler 分割と一緒にやると自然。
- [ ] **（任意）`ENGAWA_*_CMD` の分割を `shlex.split(..., posix=False)` に**（codexレビュー 7/4）＝今は `.split()` で空白入りパスの adapter 指定が壊れやすい。shell injection ではないが堅牢化。
- [ ] **pytest 移行（7/13 ユーザー判断「最初から pytest の方が良かった・テストは増えてしかるべき」）**＝現行 stdlib unittest（411件）のランナーを pytest へ。**移行コストは低い**＝pytest は unittest 形式のテストをそのまま実行できる（411件の書き直し不要）→ 段階移行: ① dev 依存に pytest を追加しランナーだけ差し替え（`pytest tests/`）② 新規テストから pytest 流儀（素の assert・fixture）③ マーカー分離＝今 skipUnless で表現している3軸（rlcard 任意依存／Windows 限定 taskkill／browser opt-in `ENGAWA_BROWSER_TESTS`）を「環境が無ければ skip」でなく「スイートとして選択/除外」（`-m browser` 等）にできる＝ジョブごとの緑が skip 混じりでなく純粋になり「skip 1 って何？」問題も構造ごと解消。**差し替え箇所**: CI `.github/workflows/ci.yml` の tests ジョブ／Stop フック（adr/0023）のテストコマンド／CLAUDE.md 原則6・TECH_RULES §9 の実行コマンド記述（＝unittest コマンドの参照が数カ所に散っているので一括で）。**注意**: ランタイムは依存ゼロ維持（pytest は dev/CI のみの依存）。急がない＝今の unittest でも回っている・テストの軸が増える兆しが出たら着手が自然なタイミング。
- [x] **デバッグモード（7/1）**＝`ENGAWA_DEBUG=1`（config・既定オフ）で `engawa.log`（gitignore）に主要ライフサイクル（種の注入/来訪/room 開閉/say/cancel/timeout/tick 分岐）を stdlib logging で吐く（`src/debuglog.py`＝薄いラッパ・各モジュール `debuglog.get("<name>")`・`setup` は composition root）。**縁側の窓/console 本文は汚さない**（別ファイル・off は NullHandler＝no-op）。LLM 判断もの（客人が季節ネタを拾うか）の切り分け土台＝「種は入れた」は `assertLogs` でテスト可能。テスト `test_debuglog`(5)＋`test_views.TestDebugConfig`(3)＋`test_scheduler`(種 placed/skip の assertLogs 1)＝全204 PASS。**残（任意）: ACP prompt/応答の詳細ログ（今回スコープ外＝冗長/機微のため既定外し）／観測点の拡充。**
  - [x] **定量観測の強化（7/2・ユーザー要望「半日回して会話タイミングを定量的に見たい」）**＝(1) タイムスタンプを秒どまり→**日付＋ミリ秒**（`YYYY-MM-DD HH:MM:SS.mmm`・`debuglog` の Formatter に `%(msecs)03d`）。(2) 観測点を追加＝`inject 茶々 (kind)`（茶々ソロつぶやき/アーク beat/ソロ応答の起点）・`user input`（人間の駆動時刻・room/solo とも）・`next beat +Ns (active=?)`（予定の間合い＝LLM 遅延と分けてペースを見る）。既存 `say who (kind)`（room 発話）と合わせ、来訪の到着→代打(muse/reply)の間隔→辞去まで msec で追える＝代打の減衰カーブや fill 間隔を数値検証できる。テスト `test_debuglog`(timestamp 書式)＋`test_scheduler`(user input/inject の assertLogs)＝全229 PASS。
- [x] モデルを config で選択可能に（6/28）＝住人(Claude)は子 env `ANTHROPIC_MODEL`（Claude Code が尊重・`opus`/`claude-opus-4-8`/`opus[1m]`）、客人(codex)は `CODEX_CONFIG` の `{"model":…}`（codex-acp が Codex 設定へマージ）。つまみ `ENGAWA_MODEL`/`ENGAWA_CODEX_MODEL`（engawa.json `model.{resident,guest}` 可・env 優先）。**未指定はアダプタ既定のまま＝現状維持**。`acp.py` `_model_env`/`_child_env`＋`config.get_str`、起動行に `茶々=<model>` 表示。ユニット10件追加(全55 PASS)。実機での実モデル切替確認はユーザー（GUI/実 adapter）。codex 側 `CODEX_CONFIG` 経由は web 調べ・実 codex での効きは未実測（無指定なら無害）
- [x] 常設テストの復帰（codexレビュー S3・6/28）＝stdlib `unittest` で GUI/ネット不要の回帰テストを `tests/` に新設。views(`collapse_ws`/`corner_xy`)・config clamp(S4)・sources(whitelist `_host_allowed`/RSS `_parse_rss_titles`/`time_of_day`/`build_context`/narration)・acp(EOF→`ConnectionError` S1)・`Scheduler`＋`CaptureView`＋fake resident/codex(user入力・cancel優先・arc結了・客人3ビート使い捨て)。実行 `python -m unittest discover -s tests -t .`（45件 PASS）。以前 PASS 記録のあった JS `node --check`/実 E2E はこの stdlib スイートには含めない（別軸）
- [x] session/cancel の実機 claude-code-acp 挙動（stopReason=cancelled が返るか）（確認済 6/27：cancelled で返る・エラーにならない）
- [ ] cmd /c の裏の node 取り残し → 本番常駐では **Job Object 化**で確実に刈る（`taskkill /PID /T /F` は実装済み＝acp.py `shutdown_process`。taskkill 失敗時/孤立子の最終保険として Job Object を被せる）
- [x] **ACP 握手失敗時の teardown 漏れ＋一時dir の刈り残し（codexレビュー S2・実装6/29）**＝`AcpAgent.spawn()` の握手失敗（timeout/EOF/error）を `except BaseException` で受け、task cancel＋`shutdown_process(proc)` を必ず走らせて再 raise。`spawn_resident`/`spawn_guest` は失敗時に temp dir を `shutil.rmtree(..., ignore_errors=True)`。`close()` も `_persona_dir` を rmtree。テスト: `test_acp.TestCloseRemovesPersonaDir`。※「cmd /c の裏の node 取り残し（Job Object 化）」は別軸で未対応のまま
- [~] **ACP request/prompt に用途別 timeout（codexレビュー S1・主要部実装6/29）**＝`ACPClient.request(timeout=)` で per-request timeout→`ACPTimeoutError`、timeout/例外いずれでも pending を pop。用途別: init120/session60/prompt240/send・cancel10 秒（`acp` 節で config 可変・初回 npx を見込み寛容）。受け側を **2系統ポリシー**で結線: 住人=ターン破棄→連続 `resident_restart_at`(既定2) で再起動→失敗で縁側を閉じる（長命セッション=文脈を一過性遅延で捨てない・ADR-0005）／客人=「急ぎの用で去る」定型退場（ハング client は二度叩かない・`sources.guest_timeout_leave`）／ゲーム=席を立ってお開き。`_tick_loop`・`run()` に保険 net。テスト: `test_acp.TestRequestTimeout` ＋ `test_scheduler.TestTimeoutRecovery`(5件)。
  - [x] **cancel 後の in-flight prompt の短い bounded wait**（実装6/29）＝`AcpAgent.cancel()` が notify 送出時に in-flight prompt の rid を掴み、`CANCEL_GRACE`(既定10s・`acp.cancel_grace`/`ENGAWA_ACP_CANCEL_GRACE`) 後も未決着なら `ACPClient.abort_pending(rid, result=cancelled)` で合成 `stopReason=cancelled` として畳む。これで adapter が cancelled 応答を握り潰しても prompt の全 timeout(240s)を待たない。**timeout でなく cancelled**（良性）にして住人の段階再起動カウンタを誤って進めない（本当のハングは続く新ターンが PROMPT_TIMEOUT で検出）。後から本物の応答が来ても `_dispatch` は pop 済みで無視＝二重決着なし。`request(on_start=)`/`abort_pending`/`_expedite_cancel` 追加、`close()` で grace タスクを畳む。テスト: `test_acp.TestCancelBoundedWait`(2件)＋`TestAbortPending`(2件)。全133 PASS。
- [ ] 茶々用 CLAUDE.md は persona/ 等の別ディレクトリに置く運用（リポジトリの CLAUDE.md と同名衝突回避）
- [ ] SQLite 永続化の実装（spec §11：residents/guests/events/messages/sessions）
- [ ] 環境イベントの「体感ナレーション」層（気温の生値→体感語、前ティック差分、時刻×気温）
- [x] 天気が文脈に入るのが遅い／ユーザーターンに無い（起動直後に話しかけると天気を捏造＝とんちんかん）（6/27 修正・案A+保持）
  - 直し: `Scheduler.weather` に保持。`run()` 起動時に1回 `fetch_weather`（入力受付前）、`_tick_loop` で毎tick更新、`on_user_input` が `build_context(self.weather)` を `user_narration(line, ctx)` へ。`user_narration` は天気を持たせるが「聞かれてないのに言い立てない」抑制付き。天気 None なら天気行を出さない（捏造材料を与えない）
  - 検証: fake 8件 PASS＋実 resident E2E（実天気「ところどころ曇り24.6℃」→茶々「ちょい曇って涼しい」で整合・tick未発火の起動直後状況で確認）
- [x] 天気の座標が大阪固定（`OSAKA_LAT/LON`）→ 利用者が変更できる仕様へ（env / 設定ファイル等）**＝実装済み（2026-07-11）**
  - 直し: `sources.WEATHER_LAT/_LON/_TZ` と `PLACE_LABEL` を config 解決（`ENGAWA_WEATHER_LAT/_LON/_WEATHER_TZ` / `ENGAWA_PLACE_LABEL`・`engawa.json[weather]`・env>json>既定）。既定は大阪＝**未指定なら現行のまま**。URL 組みは純関数 `sources._weather_url(lat,lon,tz)`（tz は URL エンコード）へ切り出しネット非依存でユニット化。地名ラベルも `ambient_narration` で `PLACE_LABEL` 連動（「地名だけ大阪」事故を回避）。lat/lon は範囲クランプ。
  - 検証: `TestWeatherLocation` 5件（既定URL=大阪/no-arg整形/京都・別TZ override＋エンコード/地名既定=大阪/地名可変）＝全384 PASS・ruff clean。
  - 注: culture.json(Inc3) 構想へ畳まず**単独 config 節**で実装（YAGNI・既存 config 主導の枠に収まる）。会話・昼夜tint の時刻はマシンのローカル時刻依存で、この weather tz とは別問題（残）。
- [ ] **設定ヘルプ／設定の発見性**（考慮点が多く「何を触ると何が起きるか」を利用者が掴みづらい・ユーザー提起 2026-07-11）
  - 現状: つまみは env(`ENGAWA_*` 20本超)＋`engawa.json` 12節(model/backend/openai/guest/timing/topic/weather/acp/absence/debug/ui/assets)まで増えた。説明は `engawa.json.sample` の `_comment` と CLAUDE.md の env 一覧に**散在＝一望できない**。優先順位(env>json>既定)・「消せばコード既定」・**連動する値**(天気の座標×地名×tz／font と /font save 等)・範囲クランプなど、正しく使うのに要る前提が点在。
  - 候補（どれか/組合せ・スコープは別途判断）: (a) アプリ内 `/config`（節ごとに**実効値＋出所**=env/json/既定を表示＝`/model`・`/font` の延長）や `/help <section>`。(b) `docs/SETTINGS.md` 一枚に節ごとの表(キー/既定/範囲/連動/env名)を集約し README・CLAUDE から張る。(c) `engawa.json.sample` を「読めるドキュメント」として正本化（既にコメント厚め＝これを軸に）。
  - 罠: **二重管理を増やさない**（sample コメントと別に表を作ると乖離＝原則7）。実効値表示にはキーの単一列挙が要る＝`config` に「登録済みキー表」を持たせれば `/config` と docs 生成の単一ソースにできる（将来）。i18n(voice)・culture.json(Inc3) とも地続き。既定を消せば現行維持、が全キーで崩れない保証も要点。
- [x] **ADR-0002 の追加点検で出た課金安全の穴／陳腐化**（点検 2026-07-11・CLAUDE_CONFIG_DIR 対応の派生。全 spawn 経路＝`acp.py` adapter spawn/taskkill と env 構築を洗った結果。🔴🟡🔵 すべて対処済み・🟢 は非問題記録）
  - 🔴 **[x] キー除去の網羅性（実質ギャップ）＝allowlist で解消（2026-07-11）**: 旧 denylist（`drop_keys` で `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` だけ pop）は、子 env が `dict(os.environ)` 継承なので `CLAUDE_CODE_USE_BEDROCK`/`_VERTEX`(＋`AWS_*`/`GOOGLE_*`)・`ANTHROPIC_AUTH_TOKEN`・`ANTHROPIC_BASE_URL` が**キー除去を迂回して従量/外部送信に行き得る**穴だった。→ `acp._child_env` を **allowlist(default-deny)** に転換（ユーザ選択＝(b)方式）: OS/ランタイム/node/npm/proxy/CA の素性だけ通し未知 env は落とす／課金ベンダー名前空間＋Bedrock/Vertex は `_is_billing_env` で**ハード拒否**（`ENGAWA_ENV_PASSTHROUGH` でも貫通不可）／逃げ道 `ENGAWA_ENV_PASSTHROUGH`（`engawa.json[auth].env_passthrough`）。**case-insensitive**（実 Windows は `SYSTEMROOT` 等大文字＝混在ケースだと取りこぼし adapter 不起動・事前チェックで捕捉→大文字正規化）。テスト `TestChildEnvAllowlist`（6件）＋既存改修＝全403 PASS・ruff clean。実 `os.environ`(82)→子40・critical 取りこぼし0・`ANTHROPIC_API_KEY` 遮断も実測。**残: 実 spawn(茶々が喋る)の GUI 実機確認**（unittest は純関数まで）。docs: ADR-0002 実装メモ2 / TECH_RULES §3 / CLAUDE.md env / engawa.json.sample[auth] を整合。
  - 🟡 **[x] ADR-0002 備考の陳腐化＝解消（2026-07-11）**: 「GPT側(OpenAI)のコストはこの決定では解決しない」を、客人 codex の OPENAI 遮断＋ADR-0026（ローカル backend＋非ローカル既定ブロック）で対応済みへ ADR 備考を更新。
  - 🔵 **[x] 「個人利用限定」の文言＝「各自 BYO サブスク」へ統一（2026-07-11）**: 公開 exe 後は言葉足らずだった（制約の実体は配布禁止でなく認証モデル＝各自 BYO・単一サブスクの共有サービス化と claude.ai ログイン同梱が ToS 不可）。ADR-0002 タイトル/決定/文言メモ・TECH_RULES §3・adr/README 索引・README(日英)・CLAUDE.md を「各自 BYO サブスク」表現へ更新。
  - 🟢 非問題（記録）: `taskkill` は env 未指定でキー入り env を継承するが API を叩かない＝無害。`agent_openai` は subprocess でなく HTTP＋remote 既定ブロック（ADR-0026 管掌）。
- [x] **全 ADR（0001–0029）横断点検＝ADR-0002 の観点を全体へ展開**（2026-07-11・4体の subagent で並行監査・決定/ステータス vs 実コードを file:line で突き合わせ・🔴ゼロ/🟡[x]一括修正/🔵[x]方針決定）
  - **🔴 実質ギャップ: ゼロ**（ADR-0002 が唯一だった）。他 ADR にコードが決定に反する/課金穴レベルは無し。
  - **🟡 陳腐化・ドリフト（11件）＝[x] 一括 docs 修正済み（2026-07-11）**: ①ステータス表記の陳腐化（0008「P4で実装予定」/0011「P3.5/P4で実装予定」/0017「Inc 実装中」/0019「実装これから」/0026 状態行「客人は ACP のまま」→ 全て実態へ更新＋adr/README 索引も）②後継 ADR への superseded/相互参照追加（0005→0027・0008→0015・0013→0029/0015）③ADR-0029 リファクタ後の名前ドリフト repoint（0014 `_topic_recent`等→`room_speakers`・0017 `_teardown_game`→`GameController._teardown`）④0010 絵柄ピボット注記（Aseprite ドット絵→Gemini スムーズ）⑤0023 CI 昇格済み注記⑥0027 実装配置注記（EventSource でなく Scheduler 直持ち）＋0006 の「S1残/要実機確認」を実装済みへ。**docs のみ・コード無変更**。
  - **🔵 [x] [0012] ＝(b) 文言を実態に合わせて決着（2026-07-11・ユーザ判断）**: 箱庭アークの実天気ゲートは**開始時のみ**で進行中の自動中断は無い（`scheduler` の source 抽選＝eligible／`BoxGardenArc.next_phase` は再ゲートせず・`sources.py`）。ADR-0012 の「降り出したら中断/反応」を**実態（開始時ゲート＋進行中フェーズは ctx 参照可・猫の転が実 raining を見る例）**に文言修正＝コード無変更。天気を捏造しない核は不変。**(a) フェーズごと再ゲート（mid-arc の強制リアクション）は将来オプションとして温存**（必要になれば前進時に再ゲートを足す）。
  - 🟢 CLEAN 多数: 0003/0004/0007/0009/0018/0020/0021/0022/0025/0028/**0029（Scheduler 実測 584 行・controller 実在・打ち切り分は不在）**。0001/0006 も核 clean。

## 発信ネタ（おまけ）
- [ ] 「AIに住人を作る」過程の記事（ACP＋人格注入、環境反応の設計）
- [ ] マルチLLM単一障害点 → ACPでベンダー非依存にした話（Mythos停止の実体験と接続）
