# ADR-0008: Codex は「役を着せて呼ぶ客人」にする（対等な対話者にしない）

- ステータス: Accepted（**P4 実装済み・召喚/自発とも実 codex E2E 検証済み**）
- 日付: 2026-06-26
- 関連: ADR-0001, ADR-0004, ADR-0007, **ADR-0015（「ナレーション一方向」を部分見直し＝有界な3人会話で客人が直接発話）**

## 背景 / 課題
ACPで Codex(GPT) も同型に繋がる（ADR-0001）。だが対等な会話相手にすると、避けたはずの「AI同士会話の不自然さ」（ADR-0004）が再燃する。

## 決定
Codexを環境イベントの一種「**来訪**」に格下げする。
- **器に役を着せて `/codex <人格>` で直接召喚**（茶々の取り次ぎを挟まない＝即・確実）。
- 客人セッションは来訪ごとに **使い捨て**（session/new → 数往復 → 破棄）。
- 滞在は **往復数で上限**（無限ループの安全弁）。
- 人格は **召喚時に prompt/system へ動的注入**（CLAUDE.md でなく）。
- 客人の出力は **ナレーション化**して茶々に渡す。〔**2026-07-11 注記: この「一方向ナレーション」は ADR-0015 で部分 Supersede**。有界な3人会話（room）では客人が自名で直接発話する（`scheduler._on_say → view.say(who, text)`）。有界・使い捨て・非常駐の核は維持。〕

## 検討した代替案
- **固定の隣人（常駐・同一人格）**: 着せ替えの自由度なし。却下。
- **茶々が取り次ぐ（空振りあり）**: 世界観は濃いが、操作の気持ちよさを優先し **直接召喚** に決定。
- **自発来訪のみ**: ユーザー主導性が無い。**自発（時刻/確率/クールダウン）＋召喚** の2系統に。

## 影響 / 帰結
- マルチベンダーの旨味（Codexが繋がる）を取りつつ、機械臭さを回避。
- 来訪フロー（到来→滞在→辞去）を自発/召喚で共通化。**P4 実装済み（召喚 `/codex`＝`scheduler._summon_guest`／自発来訪とも実 codex で E2E 検証済み）。**
- **自発来訪を箱庭アーク抽選から独立化（2026-06-29・追記）**＝当初は自発来訪をアークと同じ抽選経路（`arc_prob(0.30) × eligible 内の prob(0.05) × random.choice 競合`）に乗せていたため**三重に間引かれ**、`prob`（「tickあたりの素確率」）と実効が乖離して『殆ど来ない』状態だった。`_tick` で自発来訪を**独立判定**（from_hour × prob × cooldown だけ・アーク抽選からは guest を除外）に変更し、`prob` を実効 per-tick 率に。**有界・使い捨て・非常駐の核は不変**（頻度の調整であり原則#3に抵触しない）。

## 備考
- `/codex <自由テキスト>` はユーザー入力が客人の system に直入りする＝**配布時はプロンプトインジェクションの入口**（spec Open Question）。本人専用のV1は許容。
